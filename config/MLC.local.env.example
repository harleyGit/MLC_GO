# 可选的本机配置覆盖。复制为 MLC.local.env 后按当前电脑修改；该文件不会提交到 Git。
# Intel 当前电脑的 MySQL root 为空密码时保留等号右侧为空。
MLC_DEBUG_MYSQL_PASSWORD=

# M2Pro 电脑若共享 mysql.yaml 中的默认密码 hh109，则无需创建 MLC.local.env；
# 如需显式覆盖，可改为：MLC_DEBUG_MYSQL_PASSWORD=hh109
