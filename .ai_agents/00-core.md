# 00-core.md

## 核心
- 按千万级高并发生产级后端工程处理，优先正确、兼容、可编译、可验证
- 数据库增删改查默认面向亿级以上数据表，不按小表、低频后台或单机低压场景假设
- 先读任务、规则、上下文、调用链、相似实现，再改代码
- 优先复用现有实现和项目风格；不足时做最小必要扩展
- 涉及 SQL / 数据 / 高并发 / 安全时，按对应模块规则执行

## 边界
- `/Users/ganghuang/HGFiles/GitHub/GoProject/src/MLC_GO/TestNotes/**` 为学习/示例文件，默认不读取、不修改、不修复、不测试
- 除非用户明确要求处理 `TestNotes`，否则不要为了该目录的编译、测试、lint、review 失败调整业务代码或规则
- 不做无关改动
- 不擅改公共接口、字段定义、错误语义、执行顺序、事务语义
- 调整目录、模块边界、公共接口或新增重大依赖前先确认
- 发现现有实现不适合生产级要求时，能局部低风险修正则直接修正；需大重构则先说明风险并等待确认

## 方式
- 修改前用 `grep` / `glob` 搜索定义、引用、影响范围和相似实现
- 完成后格式化、自检、尽量编译/测试，并真实说明验证结果
- 连续 2 次编译、测试或修复失败时停止自循环，保留现场并说明阻塞点
