- [B 站级弹幕系统设计 V1](#B站级弹幕系统设计V1)
	- [Redis 的用途](#Redis的用途)
	- [亿级弹幕存储](#亿级弹幕存储)
	- [网络模型](#网络模型)
	- [千万连接的机器规模](#千万连接的机器规模)
	- [推荐方案](#推荐方案)
	- [Go 工程设计](#Go工程设计)
- [**‌ B 站级弹幕系统设计 V2**](#B站级弹幕系统设计V2)
	- [gobwas/ws 与 gnet 构建高性能 WebSocket 网关](#gobwasws-与-gnet-构建高性能-websocket-网关)
	- [gobwas/ws 的定位](#gobwasws-的定位)
	- [gobwas/ws 与 gorilla/websocket](#gobwasws-与-gorillawebsocket)
	- [gnet 与 gobwas/ws 的分工](#gnet-与-gobwasws-的分工)
	- [Go 工程接入](#go-工程接入)
	- [连接与广播设计](#连接与广播设计)
	- [技术选型](#技术选型)
	- [弹幕 V2 修改点](#弹幕V2修改点)
- [B站级弹幕系统设计 V3](#B站级弹幕系统设计V3)
	- [Go 实时消息平台架构设计](#Go实时消息平台架构设计)
	- [整体架构](#整体架构)
	- [工程与服务拆分](#工程与服务拆分)
	- [Gateway 与连接模型](#Gateway与连接模型)
	- [Room Manager 与广播模型](#RoomManager与广播模型)
	- [消息协议与发送流程](#消息协议与发送流程)
	- [Kafka、Redis 与 ClickHouse](#KafkaRedis与ClickHouse)
	- [限流与可观测性](#限流与可观测性)
	- [AI Coding Agent 提示词](#AICodingAgent提示词)
	- [生产级技术选型](#生产级技术选型)
	- [最终架构](#最终架构)
	- [弹幕V3修改点](#弹幕V3修改点)
- [B站级弹幕系统设计 V4](#B站级弹幕系统设计V4)
	- [B站弹幕 Gateway 设计](#B站弹幕-Gateway-设计)
	- [整体架构](#整体架构)
	- [工程骨架](#工程骨架)
	- [连接与房间广播](#连接与房间广播)
	- [WebSocket 与 protobuf](#WebSocket-与-protobuf)
	- [心跳、关闭与性能优化](#心跳关闭与性能优化)
	- [Kafka 与多 Gateway 路由](#Kafka-与多-Gateway-路由)
	- [压测与生产参数](#压测与生产参数)
	- [当前缺口与演进方向](#当前缺口与演进方向)
	- [弹幕V4修改点](#弹幕V4修改点)
- [B站级弹幕系统设计 V5](#B站级弹幕系统设计V5)
  - [Go 实时弹幕系统：从 gnet Gateway 到微服务架构](#Go实时弹幕系统)
	- [Gateway 核心设计](#Gateway核心设计)
	- [生产能力补齐](#生产能力补齐)
	- [Go 微服务仓库拆分](#Go微服务仓库拆分)
	- [最终技术栈与规模估算](#最终技术栈与规模估算)
	- [方案实现后的V5 目标与结论](#方案实现后的V5目标与结论)
	- [V5 时间轮架构](#V5时间轮架构)
	- [连接生命周期与并发边界](#连接生命周期与并发边界)
	- [时间轮调度算法](#时间轮调度算法)
	- [配置与容量边界](#配置与容量边界)
	- [V5 准确性与验证](#V5准确性与验证)
	- [弹幕V5修改点](#弹幕V5修改点)

---

<br/><br/><br/>

> <h2 id="B站级弹幕系统设计V1">B站级弹幕系统设计V1</h2>

目标：使用 Go 实现仿 B 站、斗鱼、虎牙的弹幕系统，支撑**千万并发连接和亿级弹幕数据**。

核心不在于单个 WebSocket 库，而在于：**连接模型、房间广播模型、消息削峰、冷热数据分离**。

---

<br/>

> <h3 id="Redis的用途">Redis 的用途</h3>

Redis 不保存全部弹幕，主要用于热门房间状态和最近弹幕等热数据。

## 热门房间状态

例如，使用 `room:10001` 保存房间在线人数和最后消费位置：

```text
room:10001

{
 online:5000000,
 last_cursor:123456
}
```

## 最近弹幕

可使用 `ZSET` 保存最近一段时间的弹幕：

```text
danmaku:10001

score=time
value=json
```

写入示例：

```text
ZADD danmaku:10001 1720000000 msg
```

只保留最近 5 分钟的数据，历史弹幕交给持久化存储。

---

<br/>

> <h3 id="亿级弹幕存储">亿级弹幕存储</h3>

不要使用 MySQL 单表保存十亿级弹幕。例如：

```text
danmaku

id
danmu_id // 字符串
video_id
user_id
content

10亿行
```

单表达到该规模后，查询和维护成本会非常高。推荐进行冷热数据分离：

- **热数据：** Redis。
- **历史数据：** ClickHouse。

ClickHouse 历史表：

```text
danmaku_history
```

主要字段：

```text
video_id
user_id
content
create_time
```

按月份分区：

```sql
PARTITION BY month(create_time)
```

按视频和创建时间排序：

```sql
ORDER BY
(
 video_id,
 create_time
)
```

该结构适合查询视频回放弹幕。

---

<br/>

> <h3 id="网络模型">网络模型</h3>

B 站级弹幕系统可采用以下网络与消息链路：

```text
Linux

epoll
 |
gnet
 |
gobwas/ws
 |
Room Engine
 |
Kafka
```

Go WebSocket 接入层选择：

```text
gnet + gobwas/ws
```

- `gnet` 提供基于 Reactor 模型的高性能事件驱动网络能力。
- `gobwas/ws` 负责 WebSocket 协议处理。
- `Room Engine` 负责房间管理和广播。
- `Kafka` 负责消息削峰和异步解耦。

---

<br/>

> <h3 id="千万连接的机器规模">千万连接的机器规模</h3>

单机经过系统参数和程序优化后，可承载约 `10万~50万` WebSocket 连接，实际容量取决于消息频率、内存占用、广播模型和硬件配置。

千万连接需要 Gateway 集群。例如使用 100 台 Gateway，每台承载 10 万连接：

```text
10,000,000 users

        |

100 gateway

每台:
100,000 connections
```

---

<br/>

> <h3 id="推荐方案">推荐方案</h3>

| 模块         | 技术选型           |
| ------------ | ------------------ |
| WebSocket 层 | `gobwas/ws`        |
| 网络模型     | `gnet`             |
| 消息队列     | `Kafka`            |
| 热状态       | `Redis Cluster`    |
| 历史弹幕     | `ClickHouse`       |
| 服务发现     | `Nacos` / `Consul` |
| RPC          | `gRPC`             |

完整架构：

```text
                 User

                  |
              WebSocket
                  |
          +---------------+
          | Danmaku GW    |
          +---------------+

                  |
              Kafka
                  |
        +----------------+
        | Room Service   |
        +----------------+

          |          |

       Redis     ClickHouse
```

对于“B 站级弹幕、千万并发、亿级数据、Go 工程”这一目标，推荐选择 **`gnet + gobwas/ws`**。

---

<br/>

> <h3 id="Go工程设计">Go 工程设计</h3>

## 弹幕入口

```text
gnet
+
gobwas/ws
```

主要负责：

- TCP 网络通信。
- WebSocket 协议处理。
- 心跳检测。
- 长连接管理。

## 内部 RPC

```text
Kitex
+
netpoll
```

主要用于服务间调用，包括用户服务、视频服务和推荐服务。

## 消息队列

```text
Kafka
```

Kafka 用于消息削峰、异步处理和服务解耦。

## 存储

- **实时热数据：** `Redis Cluster`。
- **历史弹幕：** `ClickHouse`。

## 最终架构

```text
                 Client

                   |
              WebSocket
                   |
        +----------------+
        | gnet Gateway  |
        +----------------+

                   |
              Kafka
                   |
        +----------------+
        | Room Service  |
        +----------------+

          |             |

       Redis       ClickHouse


内部服务：

Kitex
 |
netpoll
```

最终选择：`gnet + gobwas/ws`。

主要原因：

- 采用 Reactor 模型。
- 编程模型接近 Netty。
- 长连接承载能力强。
- 适合房间广播场景。
- 对网络层的控制粒度高。

**最终结论：WebSocket 库只是接入层的一部分，系统能否支撑千万并发，取决于连接承载、房间广播、消息削峰以及冷热数据分离的整体设计。**

<br/>

---

<br/><br/><br/>

> # <h1 id="B站级弹幕系统设计V2">B 站级弹幕系统设计 V2</h1>

---

<br/><br/><br/>

> <h2 id="gobwasws-与-gnet-构建高性能-websocket-网关">gobwas/ws 与 gnet 构建高性能 WebSocket 网关</h2>

`gobwas/ws` 是 Go 的高性能 WebSocket 协议库。它不像 `gorilla/websocket` 那样强调完整、易用的连接封装，而是提供更接近底层 TCP 的协议升级、帧解析和读写能力。

在弹幕、直播间聊天和大规模长连接网关中，各层职责通常是：

- **`gnet`：**负责高性能网络事件循环和 TCP 连接管理。
- **`gobwas/ws`：**负责 WebSocket Upgrade、Frame、Mask、Ping/Pong 和 Close 等协议处理。
- **业务层：**负责用户、房间、消息路由、弹幕广播和数据存储。

```text
TCP 连接
   |
WebSocket 协议解析
   |
业务消息
```

完整链路示意：

```text
                 APP
                  |
             WebSocket
                  |
        +----------------+
        | Load Balancer  |
        +----------------+
                  |
        +----------------+
        | gnet Gateway   |
        +----------------+
                  |
             gobwas/ws
                  |
            Room Manager
                  |
               Kafka
                  |
          Danmaku Service
                  |
       Redis + ClickHouse
```

---

<br/>

> <h3 id="gobwasws-的定位">gobwas/ws 的定位</h3>

## WebSocket 协议流程

客户端首先发送 HTTP Upgrade 请求：

```http
GET /chat HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: xxx
```

服务器返回 `101 Switching Protocols`：

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: xxx
```

升级完成后，连接不再传输普通 HTTP 消息，而是在 TCP 长连接上持续传输 WebSocket Frame：

```text
TCP 长连接

client
  |
server

frame frame frame
```

例如一条弹幕：

```json
{
  "userid": 10001,
  "text": "666"
}
```

实际会封装为：

```text
WebSocket Frame

+---------+
| header  |
+---------+
| payload |
+---------+
```

`gobwas/ws` 主要处理：

- HTTP Upgrade。
- WebSocket Frame 编解码。
- 客户端 Mask 解码。
- Ping/Pong 心跳帧。
- Close 控制帧。

仓库中的常用模块：

```text
gobwas/ws
├── ws
│   └── Upgrade
├── wsutil
│   ├── ReadClientData
│   └── WriteServerMessage
└── wsflate
    └── compression
```

## 性能特点

在百万级连接和高频消息场景中，关键目标是**减少内存分配、数据复制和 GC 压力**。传统处理过程可能经过多次复制：

```text
socket buffer
    ↓ copy
byte[]
    ↓ copy
message
```

底层协议库允许应用更直接地管理缓冲区和解析过程：

```text
socket buffer
      |
   直接解析
```

这不代表所有操作都会自动“零拷贝”，而是 `gobwas/ws` 提供了更低层的 API，便于调用方减少不必要的分配和复制。

---

<br/>

> <h3 id="gobwasws-与-gorillawebsocket">gobwas/ws 与 gorilla/websocket</h3>

| 对比项   | `gorilla/websocket`         | `gobwas/ws`                        |
| -------- | --------------------------- | ---------------------------------- |
| 定位     | 完整、易用的 WebSocket 封装 | 偏底层的协议工具库                 |
| 使用成本 | 较低                        | 较高                               |
| 控制能力 | 常规连接读写                | 可细粒度控制读写、缓冲区和并发模型 |
| 适用场景 | 中小型服务、快速开发        | 自定义网络栈、高性能网关           |

`gorilla/websocket` 的典型代码：

```go
conn, err := websocket.Upgrade(
	w,
	r,
	nil,
	1024,
	1024,
)
if err != nil {
	return
}

for {
	_, msg, err := conn.ReadMessage()
	if err != nil {
		return
	}
	_ = msg
}
```

常见连接模型：

```text
一个连接
   |
goroutine
   |
read loop
   |
write loop
```

该模式简单稳定，但如果每个连接都绑定独立 goroutine 和较大的缓冲区，百万连接下会带来明显的调度与内存压力。具体压力取决于实现方式，并非 `gorilla/websocket` 强制要求固定的“一连接一 goroutine”。

`gobwas/ws` 的常见使用方式：

```go
conn, _, _, err := ws.UpgradeHTTP(r, w)
if err != nil {
	return
}

msg, op, err := wsutil.ReadClientData(conn)
if err != nil {
	return
}

_ = msg
_ = op
```

应用可以自行控制：

```text
什么时候读
什么时候写
buffer 怎么管理
goroutine 怎么分配
```

**选择原则：**优先根据连接规模、团队维护成本和自定义网络模型决定，不要仅因“性能更高”就直接选择更底层的库。

---

<br/>

> <h3 id="gnet-与-gobwasws-的分工">gnet 与 gobwas/ws 的分工</h3>

`gnet` 处理 TCP 事件，但不会自动理解 WebSocket Frame。例如收到以下字节：

```text
81 85 37 fa 21 ...
```

`gobwas/ws` 可以将其解析为：

```text
Frame
opcode = text
payload = "hello"
```

组合关系：

```text
                 Client
                   |
              WebSocket
                   |
                  TCP
                   |
                 gnet
                   |
              gobwas/ws
                   |
             Danmaku Logic
```

这类似 Java 网络栈中的：

```text
Netty
  +
WebSocket Codec
```

## 不要直接混用 `gnet` 与 `ws.UpgradeHTTP`

`ws.UpgradeHTTP()` 面向 `net/http` 的 `http.Request`、`http.ResponseWriter` 和劫持后的 `net.Conn`；`gnet.Conn` 则属于事件驱动连接抽象，二者不能仅靠类型转换直接拼接。

下面的代码只能表达意图，不是完整可运行实现：

```go
// 示意代码：gnet 不是 net/http，不能直接这样调用。
func Upgrade(raw net.Conn) {
	conn, _, _, err := ws.UpgradeHTTP(request, response)
	_ = raw
	_ = conn
	_ = err
}
```

在 `gnet` 中通常需要维护连接状态，并在事件回调里完成以下流程：

```text
TCP 连接建立
      |
读取并缓存 HTTP Header
      |
验证 Upgrade: websocket
      |
计算并返回 101 响应
      |
连接切换为 WebSocket 模式
      |
增量解析 WebSocket Frame
```

`wsutil.ReadClientData()` 适合阻塞式 `io.Reader` 场景；在 `gnet` 的事件循环中直接调用阻塞式读取会破坏事件驱动模型。生产实现应使用适合增量数据的握手与 Frame 解析方式，或选择已经完成 `gnet` 集成的 WebSocket Codec。

---

<br/>

> <h3 id="go-工程接入">Go 工程接入</h3>

## 工程结构

```text
danmaku-server/
├── cmd/
│   └── gateway/
│       └── main.go
├── internal/
│   ├── websocket/
│   │   └── server.go
│   ├── room/
│   │   ├── room.go
│   │   └── manager.go
│   └── protocol/
│       └── message.go
├── go.mod
└── go.sum
```

初始化工程：

```bash
mkdir danmaku-server
cd danmaku-server
go mod init github.com/company/danmaku-server
```

安装依赖：

```bash
go get github.com/panjf2000/gnet/v2
go get github.com/gobwas/ws
```

使用 `gnet/v2`，不要引用旧模块路径 `github.com/panjf2000/gnet`。`wsutil` 已包含在 `github.com/gobwas/ws/wsutil` 中，无需单独安装。

## 最小 gnet TCP 服务器

```go
package main

import (
	"log"

	"github.com/panjf2000/gnet/v2"
)

type DanmakuServer struct {
	gnet.BuiltinEventEngine
}

func (s *DanmakuServer) OnBoot(eng gnet.Engine) gnet.Action {
	log.Println("server started")
	return gnet.None
}

func (s *DanmakuServer) OnTraffic(c gnet.Conn) gnet.Action {
	buf, err := c.Next(-1)
	if err != nil {
		return gnet.Close
	}

	log.Println(string(buf))
	return gnet.None
}

func main() {
	server := &DanmakuServer{}

	err := gnet.Run(
		server,
		"tcp://:9000",
		gnet.WithMulticore(true),
	)
	if err != nil {
		panic(err)
	}
}
```

运行：

```bash
go run .
```

此时启动的是事件驱动 TCP 服务：

```text
TCP server :9000
```

注意：这段代码尚未实现 HTTP Upgrade 和 WebSocket Frame 解析。

## 独立 `net/http` Echo 示例

如果不使用 `gnet`，可通过 `net/http + gobwas/ws` 快速构建 WebSocket Echo 服务：

```go
package main

import (
	"net/http"

	"github.com/gobwas/ws"
	"github.com/gobwas/ws/wsutil"
)

func handler(w http.ResponseWriter, r *http.Request) {
	conn, _, _, err := ws.UpgradeHTTP(r, w)
	if err != nil {
		return
	}
	defer conn.Close()

	for {
		msg, err := wsutil.ReadClientText(conn)
		if err != nil {
			return
		}

		println(string(msg))

		if err := wsutil.WriteServerText(conn, msg); err != nil {
			return
		}
	}
}

func main() {
	http.HandleFunc("/ws", handler)
	if err := http.ListenAndServe(":8080", nil); err != nil {
		panic(err)
	}
}
```

客户端发送：

```text
hello
```

服务器返回：

```text
hello
```

---

<br/>

> <h3 id="连接与广播设计">连接与广播设计</h3>

## 连接与房间模型

大规模长连接系统不应只依赖一个无分片的全局 `map[string]*websocket.Conn`。连接对象通常至少记录业务标识、底层连接、房间和待发送队列：

```go
type Client struct {
	ID        int64
	Conn      gnet.Conn
	RoomID    int64
	SendQueue chan []byte
}
```

房间模型示意：

```go
type Room struct {
	ID      int64
	Clients sync.Map
}
```

```text
Room: 10001
  |
  +-- Client
  |
  +-- Client
  |
  +-- Client
```

`sync.Map` 只是示例，不一定是高并发房间管理的最优方案。生产环境通常还需要分片、事件循环归属、背压、慢连接淘汰和连接生命周期管理。

## 弹幕消息模型

不要只传递无结构字符串，应定义明确的协议模型：

```go
type Danmaku struct {
	UserID  int64  `json:"uid"`
	RoomID  int64  `json:"room_id"`
	Content string `json:"content"`
	Time    int64  `json:"time"`
}
```

消息示例：

```json
{
  "uid": 10001,
  "room_id": 888,
  "content": "666"
}
```

## 广播模型

直接同步遍历所有连接并写入消息：

```go
for _, client := range clients {
	client.Write(msg)
}
```

在超大房间中容易被慢连接拖住，也缺少背压与批处理机制。更合理的方向是将接收、路由和广播解耦：

```text
Receive
   |
 Kafka
   |
Room Worker
   |
Batch Broadcast
```

房间广播队列示意：

```go
type Room struct {
	Broadcast chan []byte
}
```

```go
func broadcast(room *Room) {
	for msg := range room.Broadcast {
		room.Clients.Range(func(k, v any) bool {
			client := v.(*Client)
			client.Send(msg)
			return true
		})
	}
}
```

该代码只展示队列化思路。生产实现还需处理批量编码、写缓冲复用、队列上限、慢消费者、失败重试、跨节点房间路由和消息有序性。

## 规模估算

假设热门直播间存在 `300 万`在线连接、每秒产生 `10 万`条弹幕，不能简单按 `10 万 × 300 万`逐条同步广播。真实系统通常会采用：

- 房间分片与多级扇出。
- 网关本地广播，避免每条消息重复跨网络传输。
- 批量写入与共享编码结果。
- 热门房间拆分、慢连接治理和背压。
- Kafka 等消息系统进行服务解耦，而不是直接承担每个客户端的最终投递。

---

<br/>

> <h3 id="技术选型">技术选型</h3>

## WebSocket 库选择

- **中小项目、快速开发：**优先使用 `gorilla/websocket` 或 `coder/websocket` 等高层库。
- **需要自定义事件循环、缓冲区和协议处理：**考虑 `gobwas/ws`。
- **大规模连接网关：**可采用 `gnet + WebSocket Codec`，但必须正确处理增量握手、Frame 解析、背压和连接状态，不能把阻塞式 API 直接塞进事件循环。

`gobwas/ws` 不是必选项。它的价值在于提供更底层的控制能力，同时也意味着更高的实现和维护成本。

## `gnet` 与 `netpoll`

二者解决的问题不同，不应简单二选一：

- **`gnet`：**适合实现事件驱动的 TCP/WebSocket 长连接网关。
- **`netpoll`：**常作为高性能 RPC/网络库的底层能力，例如与 Kitex 组合用于内部服务通信。

网关链路：

```text
用户
 |
WebSocket
 |
Gateway
```

内部 RPC 链路：

```text
Video Service
      |
User Service
```

```text
Kitex
  |
netpoll
  |
 RPC
```

## 推荐技术栈

| 层级       | 技术方向                                            |
| ---------- | --------------------------------------------------- |
| Gateway    | `gnet/v2 + gobwas/ws` 或成熟的 gnet WebSocket Codec |
| RPC        | `Kitex + netpoll`                                   |
| MQ         | Kafka                                               |
| Cache      | Redis Cluster                                       |
| 分析存储   | ClickHouse                                          |
| 业务数据库 | MySQL，用于用户和视频元数据                         |

最终架构：

```text
gnet
  |
TCP 事件循环
  ↓
gobwas/ws
  |
WebSocket 协议
  ↓
Room Manager
  ↓
Kafka
  ↓
Redis + ClickHouse
```

**核心结论：**`gnet` 负责连接与事件循环，`gobwas/ws` 负责 WebSocket 协议，房间和消息系统负责业务广播；`netpoll` 更适合内部 RPC 网络层。大型实时系统可以同时使用这些组件，但必须按职责分层，而不是将 API 直接硬拼。

---

<br/><br/><br/>

> <h2 id="弹幕V2修改点">弹幕V2修改点</h2>
> ‌[BóYí](๑•̀ㅂ•́)و✧

已结合 `/Users/ganghuang/Desktop/Temp.md` 完成第一阶段可落地优化。当前改动重点处理单机网关的锁竞争、广播内存放大、慢客户端隔离、请求突刺和亿级 MySQL 热表点查。

未声称当前实现已经实测支撑千万连接。千万并发仍需要多实例网关、房间亲和路由、Kafka 分区、Redis Cluster、内核与网络调优以及分阶段压测共同完成。

**1. 修改了哪些文件**

- `config/base/app.yaml`
- `internal/pkg/config/hg_env_config.go`
- `internal/pkg/config/hg_video_danmaku_config_test.go`
- `internal/modules/video_danmaku/realtime/hg_gnet_server.go`
- `internal/modules/video_danmaku/realtime/hg_gnet_server_test.go`
- `internal/modules/video_danmaku/repository/hg_video_danmaku_repository.go`
- `internal/modules/video_danmaku/repository/hg_video_danmaku_repository_test.go`
- `internal/pkg/mysql/queries/hg_video_danmaku_queries.go`

前端仓库未修改。本轮保持现有 WebSocket 消息格式和前端发送行为兼容。

**2. 做了什么改动**

- 将全局房间锁拆成默认 `256` 个房间目录分片。
- 每个目录分片设置默认 `64` 个共享成员分片。
- 移除每次广播创建全房间 `[]target` 快照的逻辑。
- 广播只锁定对应视频的一个目录分片，并逐成员分片发送。
- 成员分片资源固定，不再为每个长尾视频预分配 64 个 map 和锁。
- 保留每连接出站积压统计，并将硬编码的 `64 KiB` 改为可配置。
- 慢客户端超过出站预算后主动关闭，防止热门房间广播导致 OOM。
- 慢客户端关闭延后到成员锁释放后执行，避免潜在锁重入。
- 新增 event-loop 内无锁令牌桶，在进入共享 worker 队列和 MySQL 前拒绝单连接突刺。
- 默认限流为每连接每秒 `5` 条、突发 `10` 条。
- ACK、error 和房间广播改为具体结构体序列化，移除 `map[string]any`。
- 房间路由使用内联 FNV-1a，避免每次查找创建哈希对象。
- 单连接限流时间改为 `int64` 纳秒时间戳，降低连接常驻状态体积。
- 新弹幕插入后使用 `LastInsertId()` 按聚簇主键回读。
- 不再通过 `danmaku_id` 二级唯一索引回读刚插入的亿级热表记录。
- 增加配置边界、房间分布、限流和主键回读测试。
- 增加 ACK 序列化分配基准。

新增配置：

```yaml
room_shard_count: 256
member_shard_count: 64
max_pending_bytes: 65536
command_rate_per_second: 5
command_burst: 10
```

房间和成员分片数必须是二次幂，并有明确上下限，避免错误配置制造过多锁、map 或内存常驻对象。

**3. 为什么这样改**

原实现的主要风险是：

- 所有视频共享一把 `roomsMu`。
- 热门房间每次广播都会遍历房间并复制完整目标列表。
- 百万连接房间会产生与在线人数线性增长的短命切片。
- 热门房间广播会阻塞其他视频的加入和离开。
- 慢客户端的积压预算不可配置。
- 单个恶意或异常客户端可以持续占用共享命令队列和数据库连接。
- 新记录插入后还需要经过宽字符串二级索引回读。

修改后：

- 不同视频大概率落入不同目录锁，跨视频竞争显著降低。
- 广播不再创建 O(N) 目标快照，降低大房间的 GC 和内存带宽压力。
- 长尾视频不会因每房间预分配大量分片对象产生反向内存放大。
- 限流在 gnet event-loop 内完成，不访问 Redis、MySQL，也不创建额外 goroutine。
- 过载压力不会无条件传导到 worker 队列和 MySQL。
- `LastInsertId()` 回读命中 InnoDB 聚簇主键，更适合亿级热表。

**4. 准确性检查结果**

- 保留现有 `danmaku.create`、`danmaku.ack`、`danmaku.created` 和 `error` JSON 字段。
- 保留 `requestId` 幂等与请求关联语义。
- 保留 MySQL 提交成功后才广播的语义。
- 保留 Redis 跨实例广播和 MySQL 历史恢复机制。
- 保留视频公开状态和 `close_danmaku` 校验。
- 保留 `(user_id, request_id)` 唯一键幂等。
- 保留 64 个统计分片及原事务边界。
- 房间空闲后仍会从目录中删除。
- 房间删除、立即重建和并发广播由同一个目录分片锁保护。
- 慢连接不会在成员锁持有期间直接关闭。
- `git diff --check`：通过。
- 当前工作区仅包含本轮 8 个目标文件的修改，没有提交，也没有推送。

**5. 潜在影响**

- 单连接超过默认发送频率时将收到：

```json
{
  "type": "error",
  "requestId": "...",
  "data": {
    "code": "rate_limited",
    "message": "弹幕发送过于频繁，请稍后重试"
  }
}
```

- 这是明确的 WebSocket 业务拒绝，现有前端不会继续 HTTP 回退。
- 配置缺少新增字段时，应用会因资源边界校验失败而拒绝启动。项目基础配置已补齐；独立覆盖整个 `video_danmaku` 配置块的外部环境需要同步增加这些字段。
- 广播仍然必须执行 O(N) 连接写入，这是向 N 个客户端推送数据本身不可消除的成本。
- 当前每连接仍调用一次 `AsyncWrite`。下一阶段需要按 gnet event-loop 建立有界批量出站队列，进一步减少跨 event-loop 调度。
- Redis 仍使用单个全局 Pub/Sub 频道，网关实例数增加后会产生全量消息放大。
- MySQL 统计分片更新仍在创建事务内，热门视频极端写入时仍可能出现行锁竞争。
- 没有执行真实百万或千万连接压测，不能将代码设计优化等同于容量验证。

**6. 格式化/编译/测试说明**

已执行：

```text
gofmt
go test ./internal/modules/video_danmaku/... ./internal/pkg/config ./internal/pkg/mysql/queries
go vet ./internal/modules/video_danmaku/... ./internal/pkg/config ./internal/pkg/mysql/queries
go test -race ./internal/modules/video_danmaku/realtime ./internal/modules/video_danmaku/repository
go test -run '^$' -bench 'BenchmarkCommandAckPayload' -benchmem ./internal/modules/video_danmaku/realtime
git diff --check
```

结果：

- 受影响 Go 测试：通过。
- `go vet`：通过。
- realtime 和 repository race test：通过。
- 配置边界测试：通过。
- 房间目录分布和限流测试连续执行 20 次：通过。
- SQL 主键回读测试：通过。
- 差异空白检查：通过。

当前 ACK 序列化基准，Apple M2 Pro：

```text
BenchmarkCommandAckPayload-10
3259953 iterations
363.5 ns/op
400 B/op
2 allocs/op
```

该基准只覆盖 ACK JSON 生成，不代表端到端网络广播吞吐。

**7. 后续生产级优化建议**

1. 将 Redis 单一全局频道改为本地活跃房间动态订阅，避免每台网关接收全平台所有弹幕。
2. 按 gnet event-loop 建立有界广播队列，一次唤醒批量写多个连接，减少每连接一次 `AsyncWrite` 调度。
3. 增加命令队列深度、拒绝数、慢客户端关闭数、广播接收人数、锁等待和房间数量指标。
4. 修复现有 Outbox 每秒实际约 8 条的吞吐限制，再接入 `video.danmaku.created.v1` Kafka 事件。
5. 将计数更新移出弹幕创建事务，由 Kafka consumer 批量维护 Redis/MySQL 投影。
6. 为视频可弹幕状态增加短 TTL 本地加 Redis 缓存，降低创建和列表接口的重复 JOIN。
7. 引入按 `video_id` 的 Kafka partition 和网关 room affinity，避免广播全网扩散。
8. 亿级以上历史数据采用 MySQL 热数据分片、ClickHouse 冷历史和有界归档，不依赖单张 InnoDB 表无限增长。
9. 使用 10 万、50 万、100 万连接分阶段压测，采集单连接内存、广播 P99、GC、mutex/block profile、Redis 带宽和 MySQL 锁等待，再决定单机安全连接上限。



<br/>

---

<br/><br/><br/>
> # <h1 id="B站级弹幕系统设计V3">B 站级弹幕系统设计 V3</h1>


***
<br/><br/><br/>

> <h2 id="Go实时消息平台架构设计">Go 实时消息平台架构设计</h2>

面向 B 站弹幕、直播聊天室、游戏实时消息、IM 群聊和点赞通知等场景，如果目标包括**千万级 WebSocket 长连接、亿级弹幕数据、毫秒级广播和持续扩展能力**，就不应只设计一个 WebSocket Server，而应建设完整的**实时消息平台（Realtime Messaging Platform）**。

**核心原则：** Gateway 只维护连接和转发消息；Room Worker 负责本地实时广播；Kafka 承担异步解耦和持久化链路；Redis 保存热点状态；ClickHouse 存储海量历史消息。

***
<br/>

> <h3 id="整体架构">整体架构</h3>

```text
                         Client
                            |
                            |
                    WebSocket Connection
                            |
                            |
                +------------------------+
                |   Danmaku Gateway      |
                |   (gnet + gobwas/ws)   |
                +------------------------+
                            |
                            |
                    Room Router
                            |
             +--------------+--------------+
             |                             |
             |                             |
       Room Worker Pool              Connection Manager
             |
             |
          Kafka Producer
             |
             |
        Kafka Topic
             |
             |
 +-----------------------------+
 |                             |
 |                             |
Danmaku Consumer          Analytics Consumer
 |
 |
 +-------------+
 |
 |
Redis Cluster
 |
 |
ClickHouse
 |
 |
MySQL
```

这套架构将实时广播与异步数据处理分离：消息进入 Gateway 后由 Room Worker 直接广播，同时投递 Kafka，供存储、分析等消费者异步处理。

***
<br/>

> <h3 id="工程与服务拆分">工程与服务拆分</h3>

生产环境不建议使用单体架构，可按业务职责拆分：

```text
bilibili-realtime/
├── gateway
├── room-service
├── danmaku-service
├── user-service
├── online-service
├── analytics-service
├── common
└── deploy
```

- **Gateway：** 基于 `gnet`、`gobwas/ws` 维护海量长连接。
- **Room Service：** 基于 `goroutine + channel` 管理房间状态、消息排序和广播。
- **Danmaku Service：** 消费 Kafka 消息并写入 ClickHouse 等存储。
- **User Service：** 提供用户、鉴权及相关业务能力。
- **Online Service：** 统计在线连接数或近似去重用户数。
- **Analytics Service：** 异步消费消息并完成指标分析。

***
<br/>

> <h3 id="Gateway与连接模型">Gateway 与连接模型</h3>

### Gateway 职责

Gateway 只负责：

1. TCP 连接。
2. WebSocket 协议处理。
3. 心跳检测。
4. 登录鉴权。
5. 加入或退出房间。
6. 消息转发。

Gateway 不应直接写数据库、操作 Kafka 或查询完整用户资料，避免连接层被业务逻辑阻塞。可参考以下目录：

```text
gateway/
├── main.go
├── server.go
├── connection.go
├── websocket.go
├── heartbeat.go
├── router.go
└── middleware/
```

---
<br/>

### Connection 模型

千万级连接不适合集中存放在一个全局 `map[userID]*Conn` 中，应按 Gateway 实例、房间或分片组织连接，降低锁竞争和单点内存压力。

```go
type Connection struct {
    ID            int64
    UserID        int64
    RoomID        int64
    Conn          gnet.Conn
    SendQueue     chan []byte
    LastHeartbeat int64
}
```

连接管理器可维护房间分片：

```go
type ConnectionManager struct {
    rooms sync.Map
}
```

```text
RoomManager

room:10001
       |
       |
       +-- conn
       |
       +-- conn
       |
       +-- conn
```

实际生产中还需限制 `SendQueue` 容量，并通过连接分片、事件循环归属或单线程房间 Worker 控制并发访问。

***
<br/>

> <h3 id="RoomManager与广播模型">Room Manager 与广播模型</h3>

一个视频或直播间对应一个 `room_id`。例如视频 `BV1xxxx` 可映射到 `room:888888`：

```go
type Room struct {
    ID           int64
    Clients      sync.Map
    MessageQueue chan *Message
}
```

热门房间可分配独立 Worker，由单独的 goroutine 处理广播、限流和排序：

```text
热门直播

room worker

goroutine

负责:
- 广播
- 限流
- 排序
```

---
<br/>

### 广播模型

不要在广播主路径中同步向所有客户端写数据：

```go
for client := range clients {
    client.Write(msg)
}
```

任意慢客户端都可能阻塞整个房间。正确方式是为每个连接维护独立的有界发送队列，广播过程只执行非阻塞投递：

```go
type Connection struct {
    SendQueue chan []byte
}
```

```go
func Broadcast(msg []byte) {
    room.Clients.Range(func(_, v any) bool {
        conn := v.(*Connection)

        select {
        case conn.SendQueue <- msg:
        default:
            // 丢弃慢客户端
        }

        return true
    })
}
```

队列满时可按业务要求丢弃消息、合并消息或断开慢客户端，避免单个连接拖垮整个房间。百万级热门房间还需进一步采用房间分片、广播树或多级 Fan-out，单机遍历所有连接并不足以支撑跨节点广播。

***
<br/>

> <h3 id="消息协议与发送流程">消息协议与发送流程</h3>

### Protobuf 二进制协议

大规模实时消息不建议使用 JSON 作为核心传输格式，因为字段名和文本解析会增加带宽及 CPU 消耗。可使用 Protobuf：

```protobuf
message Danmaku {
  int64 user_id = 1;
  int64 room_id = 2;
  string content = 3;
  int64 timestamp = 4;
}
```

```text
protobuf
      |
      |
Go struct
```

客户端通过 WebSocket Binary Frame 发送 Protobuf 字节：

```text
Binary Frame

protobuf bytes
```

而不是发送以下 JSON：

```json
{
  "text": "666"
}
```

实际协议还应包含消息类型、协议版本、请求 ID、压缩标记和载荷长度等信封字段。

---
<br/>

### 发送链路

用户发送弹幕后的异步数据链路：

```text
APP
 |
 |
WebSocket
 |
 |
Gateway
 |
 |
Room Worker
 |
 |
Kafka
 |
 |
Consumer
 |
 |
Redis
```

**广播不要经过 Kafka。** Kafka 会增加实时广播延迟，应由 Room Worker 在本地或实时 Fan-out 网络中直接广播；Kafka 负责异步存储、分析和事件分发。

```text
              send

              |
              |

Gateway

              |

Room Worker

              |

100万人连接
```

典型处理顺序：

1. Gateway 完成鉴权、协议解析和基础校验。
2. Room Worker 执行限流、排序并实时广播。
3. 消息异步投递 Kafka。
4. 消费者分别更新热点数据、持久化历史消息和生成分析指标。

***
<br/>

> <h3 id="KafkaRedis与ClickHouse">Kafka、Redis 与 ClickHouse</h3>

### Kafka：异步消息总线

Topic：

```text
danmaku_message
```

按照 `room_id` 计算 Partition：

```text
partition = hash(roomID) % N
```

同一个房间的消息进入同一 Partition，可在 Kafka Partition 范围内保持顺序。

```go
type KafkaMessage struct {
    RoomID   int64
    UserID   int64
    Content  string
    Timestamp int64
}
```

---
<br/>

### Redis：热点状态

Redis 不保存所有历史弹幕，只保存热点数据。

**在线人数：**

```text
Key:   room:online:{roomID}
Value: 500000
```

连接建立和断开时可使用计数器维护在线连接数。`HyperLogLog` 适合统计近似去重用户数，但不适合要求精确增减的实时在线人数。

**最近弹幕：**

使用 Redis Stream 保存房间最近的 `1000` 条弹幕：

```text
room:danmaku:{roomID}
```

```sh
XADD room:danmaku:888 *
```

写入后可结合 `MAXLEN` 或定期裁剪控制 Stream 长度。

---
<br/>

### ClickHouse：历史消息

亿级弹幕历史数据不宜全部写入 MySQL，可使用 ClickHouse：

```sql
CREATE TABLE danmaku_history
(
    id UInt64,
    room_id UInt64,
    user_id UInt64,
    content String,
    create_time DateTime
)
ENGINE = MergeTree
PARTITION BY toYYYYMM(create_time)
ORDER BY
(
    room_id,
    create_time
);
```

按房间和时间查询视频回放弹幕：

```sql
SELECT *
FROM danmaku_history
WHERE room_id = 888
ORDER BY create_time;
```

生产环境通常由 Kafka Consumer 聚合批量写入 ClickHouse，避免逐条插入。

***
<br/>

> <h3 id="限流与可观测性">限流与可观测性</h3>

### 多级限流

为防止刷屏，可使用本地令牌桶与 Redis 分布式限流组合控制：

```text
danmaku:rate:{userID}
```

- **用户级：** 例如每个用户 `5 条/秒`。
- **房间级：** 例如每个房间 `10 万条/秒`。
- **连接级：** 限制单连接消息频率、帧大小和发送队列长度。
- **系统级：** 过载时执行降级、采样、丢弃低优先级消息或拒绝新连接。

### 可观测性

至少监控以下指标：

- 当前连接数、连接建立和断开速率。
- 各房间消息吞吐量与广播延迟。
- 发送队列长度、队列满次数和慢客户端数量。
- Kafka Producer 延迟、积压量和消费失败率。
- Redis、ClickHouse 的写入延迟与错误率。
- Goroutine 数量、GC、内存和网络流量。

使用 Prometheus 采集指标，OpenTelemetry 记录跨服务链路，并支持优雅关闭和连接迁移。

***
<br/>

> <h3 id="AICodingAgent提示词">AI Coding Agent 提示词</h3>

可将以下需求交给 Claude Code、Codex、Cursor 等 AI Coding Agent 生成项目骨架：

```text
请生成一个 Go 微服务项目：

名称：
bilibili-realtime

技术：
- Go 1.24
- gnet/v2
- gobwas/ws
- Kafka franz-go
- Redis go-redis/v9
- ClickHouse driver
- protobuf

实现：
1. WebSocket Gateway
2. Room Manager
3. Connection Manager
4. Kafka Producer
5. Kafka Consumer
6. Redis Online Counter
7. ClickHouse Storage

要求：
- 按 100 万连接目标设计
- 使用事件驱动模型
- 尽量减少共享锁竞争
- 对高频临时对象合理使用 sync.Pool
- 使用 protobuf binary protocol
- 支持优雅关闭
- 接入 Prometheus metrics
```

“无锁设计”和 `sync.Pool` 不应作为绝对目标，应先保证连接归属、并发模型和内存生命周期正确，再通过压测决定优化方案。

***
<br/>

> <h3 id="生产级技术选型">生产级技术选型</h3>

| 模块 | 技术 |
| --- | --- |
| TCP 事件循环 | gnet/v2 |
| WebSocket | gobwas/ws |
| 协议 | Protobuf |
| RPC | gRPC / Kitex |
| MQ | Kafka |
| 热点状态 | Redis Cluster |
| 历史存储 | ClickHouse |
| 监控 | Prometheus |
| 链路追踪 | OpenTelemetry |
| 部署 | Kubernetes |

容量不能只按“连接数”评估，还需同时计算每台 Gateway 的文件描述符、内存、心跳流量、出站带宽、热门房间扇出量和跨节点广播成本。

***
<br/>

> <h3 id="最终架构">最终架构</h3>

```text
                 App

                  |

            WebSocket

                  |

        +----------------+
        | gnet Gateway   |
        +----------------+

                  |

          Room Manager

                  |

          Local Broadcast

                  |

        Millions Connections



                  |

             Kafka Async

                  |

        Danmaku Storage
          /             \

     Redis             ClickHouse
```

该架构的职责边界如下：

- **Gateway：** 长连接、协议、心跳、鉴权和转发。
- **Room Worker：** 实时广播、排序、限流和慢客户端隔离。
- **Kafka：** 异步解耦存储、分析和扩展业务。
- **Redis：** 在线状态、最近消息等热点数据。
- **ClickHouse：** 海量历史消息和回放查询。

**核心结论：实时链路追求低延迟，持久化链路追求可靠解耦，两者必须分离。**



---
<br/><br/><br/>
> <h2 id="弹幕V3修改点">弹幕V3修改点</h2>

‌BóYí (๑•̀ㅂ•́)و✧
1. 修改了哪些文件
核心网关与实时路由：
- internal/modules/video_danmaku/realtime/hg_gnet_server.go
- internal/modules/video_danmaku/realtime/hg_room_router.go
- internal/modules/video_danmaku/realtime/hg_gnet_server_test.go
- internal/modules/video_danmaku/module/hg_video_danmaku_module.go
Kafka、Outbox 与消费链路：
- internal/events/video_danmaku/hg_video_danmaku_event.go
- internal/consumer/danmaku/hg_danmaku_consumer.go
- internal/consumer/danmaku/hg_recent_projector.go
- internal/consumer/danmaku/hg_danmaku_consumer_test.go
- internal/infrastructure/kafka/hg_runtime.go
- internal/pkg/kafka/hg_config.go
- internal/pkg/config/hg_kafka_config.go
- internal/outbox/hg_outbox_dispatcher.go
- hg_kafka_application.go
数据存储与 Redis：
- internal/modules/video_danmaku/repository/hg_video_danmaku_repository.go
- internal/pkg/clickhouse/hg_client.go
- internal/pkg/clickhouse/hg_client_test.go
- internal/pkg/redis/hg_redis_key.go
- internal/pkg/redis/hg_redis_script.go
- internal/pkg/redis/hg_video_danmaku_key_test.go
- deployments/clickhouse/002_video_danmaku_history.sql
配置、脚本与文档：
- config/base/app.yaml
- config/base/clickhouse.yaml
- config/debug/kafka.yaml
- config/pre/kafka.yaml
- config/prod/kafka.yaml
- internal/pkg/config/hg_env_config.go
- scripts/kafka_init.sh
- scripts/kafka_init_test.sh
- HG_Docuemnts/视频弹幕千万并发与亿级存储部署.md
工作区原有的 HG_Docuemnts/视频弹幕版本.md 修改未由本轮产生，未覆盖或回退。
2. 做了什么改动
- 将 Redis 全局广播频道改为按视频动态频道：video:danmaku:broadcast:{videoID}:v2。
- 本实例首个房间连接建立时订阅，最后一个连接离开时异步退订。
- Redis 订阅线程不再直接遍历热门房间，而是投递到固定 64 个广播分片 worker。
- 同一视频稳定进入同一个广播 worker，保持单实例内广播顺序。
- 广播队列有容量上限，过载时丢弃可恢复的实时副本，不阻塞 Redis 订阅和 gnet event-loop。
- 保留现有房间目录、成员分片、慢客户端字节预算及 JSON WebSocket 协议。
- MySQL 弹幕记录和 Outbox 事件现在可以在同一事务中提交。
- 新增稳定事件 video.danmaku.created，Kafka partition key 为 video_id。
- 新增专用 Kafka Topic mlc.video.danmaku.created.v1，默认 96 partitions、3 replicas。
- Kafka Consumer 按同一 partition 的有界批次写入 ClickHouse。
- 新增 ClickHouse 表 video_danmaku_history，按月分区，排序键为：
(video_id, progress_ms, created_at, danmaku_id)。
- ClickHouse 使用 ReplacingMergeTree(ingested_at)，支持 Kafka 至少一次投递和历史重放。
- 新增 ClickHouse 历史查询能力，限制为最大 5 分钟窗口、1000 条、复合游标分页，禁止深度 OFFSET。
- Kafka 消费成功后，将最近弹幕投影到 Redis Stream。
- Redis Stream 默认仅保留最近约 1000 条，避免将完整历史堆入 Redis。
- Redis Lua 使用 topic:partition -> offset 水位实现永久幂等投影。
- Stream key 和 offset key 使用相同 {videoID} hash tag，兼容 Redis Cluster slot 约束。
- Outbox dispatcher 从 8 worker 提升到固定 64 worker。
- Outbox 有积压时持续排空，只有领取不到事件时才等待轮询间隔。
- 新增完整部署、灰度、回滚、房间亲和、容量计算和压测说明。
3. 为什么这样改
原有实现存在两个主要扩展瓶颈：
- 每个 Gateway 都订阅全平台广播频道，实例越多，Redis 流量和反序列化工作量越大。
- MySQL 单表不能作为亿万至百亿级弹幕历史的长期唯一存储。
新链路形成以下职责边界：
实时广播：
Client -> gnet Gateway -> local room broadcast
                       -> Redis room channel -> other subscribed gateways

可靠历史：
MySQL transaction -> Outbox -> Kafka
                           -> ClickHouse complete history
                           -> Redis recent Stream
这样可以通过增加 Gateway Pod 横向扩展连接数，通过 Kafka partition 和 Consumer 扩展历史写入吞吐，并将大规模时间线查询转移到 ClickHouse。
4. 准确性检查结果
已确认：
- 同一视频的 Redis 广播、Stream 和 offset key 具有一致的 Cluster hash tag。
- 首个连接只有在 Redis 房间订阅成功后才完成 WebSocket 加入。
- 并发第二个连接不会提前绕过首个订阅确认。
- gnet event-loop 不执行退订 Redis I/O。
- Redis 收包不会同步阻塞在百万连接房间广播上。
- MySQL 数据与 Outbox 事件处于同一事务。
- 幂等重放不会重复生成 Outbox 事件。
- Kafka Consumer 先写 ClickHouse，再推进 Redis offset 投影，失败时不提交 Kafka offset。
- ClickHouse 使用批量 JSONEachRow，不是逐条 HTTP 请求。
- ClickHouse 查询使用稳定复合游标，不使用深分页。
- 现有 HTTP、JSON WebSocket、ACK 和 MySQL 幂等语义未改变。
- git diff --check 通过。
- 未发现密钥、密码或临时编译产物写入仓库。
5. 潜在影响
- 弹幕历史 Kafka Consumer 在 debug、pre、prod 中默认保持 enabled: false。
- 启用消费者前必须先执行 deployments/clickhouse/002_video_danmaku_history.sql。
- 当前 HTTP 时间窗查询仍读取 MySQL 热数据，ClickHouse 查询能力已经实现，但尚未强制切换外部查询路径。
- Redis Pub/Sub 仍是实时非可靠传输；断线恢复依赖 MySQL、Redis recent 或 ClickHouse。
- 当前仍保持同步 MySQL 权威创建语义，以兼容现有 ACK。Kafka 和 ClickHouse承担异步历史归档，而不是将旧客户端 ACK 改成“Kafka 已接收”。
- 96 partitions 是默认部署值，最终数量必须根据 Kafka broker 数、写入量和 Consumer 并发确定。
- 单个超级热门视频仍会集中到一个 Kafka partition。达到单 partition 上限后需要 lane 分片，并明确是否接受跨 lane 弱顺序。
- ClickHouse 默认保留 1825 天，生产需根据磁盘、合规和备份策略调整。
- 本轮没有执行真实 Kafka、Redis Cluster、ClickHouse 集成环境写入，也没有执行百万连接压测。
- 当前结果应描述为“已实现支持横向扩展的千万连接和亿级历史架构路径”，不能描述为“当前单机已经实测支持千万连接”。
6. 格式化、编译、测试说明
已执行并通过：
gofmt
go test ./internal/modules/video_danmaku/...
go test ./internal/consumer/danmaku
go test ./internal/infrastructure/kafka
go test ./internal/outbox
go test ./internal/pkg/config
go test ./internal/pkg/clickhouse
go test ./internal/pkg/kafka
go test ./internal/pkg/redis
Race 检查通过：
go test -race ./internal/modules/video_danmaku/realtime
go test -race ./internal/consumer/danmaku
go test -race ./internal/outbox
静态检查通过：
go vet ./internal/modules/video_danmaku/... \
  ./internal/consumer/danmaku \
  ./internal/infrastructure/kafka \
  ./internal/outbox \
  ./internal/pkg/config \
  ./internal/pkg/clickhouse \
  ./internal/pkg/kafka \
  ./internal/pkg/redis
脚本验证通过：
bash -n scripts/kafka_init.sh scripts/kafka_init_test.sh
bash scripts/kafka_init_test.sh
结果：
[PASS] Kafka Topic 拓扑校验通过
本轮未提交、未 push。
7. 后续生产级优化建议
1. 按文档先创建 Kafka Topic 和 ClickHouse 表，再从单副本灰度启用弹幕历史 Consumer。
2. 为 WebSocket Ingress 增加基于 videoId 的 consistent hash，降低同房间跨 Pod 广播比例。
3. 增加弹幕专属 Prometheus 指标和告警，包括广播队列丢弃、Outbox pending/dead、Kafka lag、ClickHouse 写延迟和 Redis 投影失败。
4. 完成 MySQL 与 ClickHouse 双写对账及历史回灌后，将旧时间窗查询路由到 ClickHouse。
5. 使用 10 万、50 万、100 万连接分阶段压测，采集单连接内存、广播 P99、GC、网络带宽、Kafka partition 热点和 ClickHouse merge 压力，再确定每 Pod 安全连接数及千万连接所需副本数。


<br/>

***
<br/><br/><br/>
># <h1 id="B站级弹幕系统设计V4">B站级弹幕系统设计 V4</h1>

***
<br/><br/><br/>

> <h2 id="B站弹幕-Gateway-设计">B站弹幕 Gateway 设计</h2>

本文整理一个基于 `gnet/v2 + gobwas/ws + protobuf + Kafka` 的弹幕 Gateway 工程骨架，目标包括长连接管理、房间广播、心跳检测、异步写队列、优雅关闭及多节点扩展。

> **容量说明：** 单节点百万连接、集群千万连接均是架构目标，不是仅凭以下代码即可保证的结果。实际容量取决于协议实现、内存模型、操作系统参数、网络带宽、消息频率和压测结果。

**核心职责：**

- `gnet/v2`：基于 Reactor/EventLoop 处理大量 TCP 连接。
- `gobwas/ws`：处理 WebSocket Upgrade 和 Frame。
- `protobuf`：提供紧凑的二进制消息协议。
- `RoomManager`：维护房间和本地连接并执行广播。
- `Kafka`：异步传递弹幕事件和跨节点广播消息。
- `Redis`：可用于 Gateway 注册发现、房间路由等集群元数据。
- `ClickHouse`：存储和查询历史弹幕、分析数据。

<br/>

> <h3 id="整体架构">整体架构</h3>

```text
                         Client
                           |
                     WebSocket/TCP
                           |
                    Load Balancer
                           |
       +-------------------+----------------+
       |                   |                |
      GW1                 GW2              GW3
       |                   |                |
       +-------------------+----------------+
                           |
                    Kafka Cluster
                           |
              +------------+-------------+
              |                          |
        Danmaku Service              Analytics
              |
        Redis Cluster
              |
        ClickHouse
```

单节点内部链路：

```text
                 APP
                  |
             WebSocket
                  |
        +----------------+
        | gnet Gateway   |
        +----------------+
                  |
          ConnectionManager
                  |
             RoomManager
                  |
        +----------------+
        | Broadcast      |
        +----------------+
             |       |
      WebSocket      Kafka
       Clients         |
                Danmaku Service
                       |
              Redis + ClickHouse
```

***
<br/>

> <h3 id="工程骨架">工程骨架</h3>

```text
danmaku-gateway/
├── go.mod
├── cmd
│   └── gateway
│       └── main.go
├── internal
│   ├── gateway
│   │   ├── client.go
│   │   ├── server.go
│   │   └── handler.go
│   ├── websocket
│   │   ├── upgrade.go
│   │   └── frame.go
│   ├── connection
│   │   ├── connection.go
│   │   └── manager.go
│   ├── room
│   │   ├── room.go
│   │   └── manager.go
│   ├── heartbeat
│   │   └── heartbeat.go
│   ├── kafka
│   │   └── producer.go
│   └── pool
│       └── buffer.go
└── proto
    └── danmaku.proto
```

### 依赖

```bash
go mod init github.com/company/danmaku-gateway
go get github.com/panjf2000/gnet/v2
go get github.com/gobwas/ws
go get github.com/gobwas/ws/wsutil
go get google.golang.org/protobuf
go get github.com/segmentio/kafka-go
go get github.com/redis/go-redis/v9
```

`go.mod` 示例：

```go
module github.com/company/danmaku

go 1.24

require (
	github.com/gobwas/ws v1.4.0
	github.com/panjf2000/gnet/v2 v2.8.12
	github.com/segmentio/kafka-go
	google.golang.org/protobuf
)
```

> 以上依赖版本仅是原始示例，实际工程应使用完整合法的模块版本，并通过 `go mod tidy` 固化依赖。

### Gateway 入口

```go
package main

import (
	"log"

	"github.com/company/danmaku/internal/gateway"
	"github.com/panjf2000/gnet/v2"
)

func main() {
	server := gateway.New()

	err := gnet.Run(
		server,
		"tcp://:9000",
		gnet.WithMulticore(true),
		gnet.WithReusePort(true),
	)
	if err != nil {
		log.Fatal(err)
	}
}
```

### gnet Server 骨架

```go
package gateway

import (
	"log"

	"github.com/company/danmaku/internal/kafka"
	"github.com/company/danmaku/internal/room"
	"github.com/panjf2000/gnet/v2"
)

type Server struct {
	gnet.BuiltinEventEngine

	Rooms *room.Manager
	Kafka *kafka.Producer
}

func New() *Server {
	return &Server{
		Rooms: room.NewManager(),
		Kafka: kafka.NewProducer(),
	}
}

func (s *Server) OnBoot(eng gnet.Engine) {
	log.Println("gateway start")
}
```

***
<br/>

> <h3 id="连接与房间广播">连接与房间广播</h3>

### Connection

连接对象保存业务身份、底层连接、最后活跃时间和发送队列：

```go
package connection

import (
	"sync/atomic"

	"github.com/panjf2000/gnet/v2"
)

var counter uint64

type Connection struct {
	ID         uint64
	UserID     int64
	RoomID     int64
	Conn       gnet.Conn
	SendQueue  chan []byte
	LastActive int64
	Close      chan struct{}
}

func New(c gnet.Conn) *Connection {
	return &Connection{
		ID:        atomic.AddUint64(&counter, 1),
		Conn:      c,
		SendQueue: make(chan []byte, 1024),
		Close:     make(chan struct{}),
	}
}
```

不应在房间广播路径中对每个慢客户端直接执行同步写：

```go
conn.Write(msg)
```

若用户 A 的网络写入耗时 `100ms`，同步广播可能拖慢整个房间。推荐链路：

```text
Room
  |
SendQueue
  |
write loop
  |
socket
```

发送队列必须有界，并定义慢客户端策略，例如丢弃消息、断开连接或按优先级降级，否则积压最终会转化为内存问题。

### Connection Manager

```go
package connection

import "sync"

type Manager struct {
	conns sync.Map
}

func NewManager() *Manager {
	return &Manager{}
}

func (m *Manager) Add(c *Connection) {
	m.conns.Store(c.ID, c)
}

func (m *Manager) Remove(id uint64) {
	m.conns.Delete(id)
}

func (m *Manager) Range(fn func(*Connection)) {
	m.conns.Range(func(_, value any) bool {
		fn(value.(*Connection))
		return true
	})
}
```

### Room 与 Room Manager

一个视频或直播间映射为一个 `Room`，例如 `BV1xxx -> roomID=10001`。

```go
package room

import "sync"

type Client interface {
	Send([]byte) error
}

type Room struct {
	ID      int64
	clients sync.Map
}

func NewRoom(id int64) *Room {
	return &Room{ID: id}
}

func (r *Room) Join(id uint64, client Client) {
	r.clients.Store(id, client)
}

func (r *Room) Leave(id uint64) {
	r.clients.Delete(id)
}

func (r *Room) Broadcast(data []byte) {
	r.clients.Range(func(_, value any) bool {
		client := value.(Client)
		if err := client.Send(data); err != nil {
			// TODO remove
		}
		return true
	})
}

type Manager struct {
	rooms sync.Map
}

func NewManager() *Manager {
	return &Manager{}
}

func (m *Manager) Get(id int64) *Room {
	value, _ := m.rooms.LoadOrStore(id, NewRoom(id))
	return value.(*Room)
}
```

若 `Room` 直接保存 `*connection.Connection`，使用有界 `SendQueue` 时可采用非阻塞写入：

```go
func (r *Room) Broadcast(msg []byte) {
	r.Clients.Range(func(_, value any) bool {
		conn := value.(*connection.Connection)

		select {
		case conn.SendQueue <- msg:
		default:
			// 慢客户端丢弃
		}

		return true
	})
}
```

### gnet Client 写入

```go
package gateway

import "github.com/panjf2000/gnet/v2"

type Client struct {
	ID     uint64
	RoomID int64
	UserID int64
	Conn   gnet.Conn
}

func (c *Client) Send(data []byte) error {
	return c.Conn.AsyncWrite(data)
}
```

`AsyncWrite` 只是基础骨架。高负载场景仍需发送队列、批量写、背压和慢连接淘汰策略：

```text
每连接:
SendQueue
    |
write loop
    |
AsyncWrite
```

***
<br/>

> <h3 id="WebSocket-与-protobuf">WebSocket 与 protobuf</h3>

### WebSocket Upgrade 状态

`gobwas/ws` 常见接口基于 `net/http` 或 `net.Conn`，而 `gnet` 的入口是 TCP EventLoop：

```text
TCP EventLoop
  |
OnTraffic()
```

因此需要在事件回调中自行维护协议状态：

```text
HTTP Upgrade
      ↓
101 Switching Protocols
      ↓
WebSocket Frame
```

```go
type ConnState int

const (
	HTTPUpgrade ConnState = iota
	WebSocketConnected
)

type Client struct {
	State  ConnState
	Conn   gnet.Conn
	UserID int64
	RoomID int64
}
```

### Upgrade Accept Key

```go
package websocket

import (
	"crypto/sha1"
	"encoding/base64"
)

func AcceptKey(key string) string {
	h := sha1.New()
	h.Write([]byte(key + "258EAFA5-E914-47DA-95CA-C5AB0DC85B11"))
	return base64.StdEncoding.EncodeToString(h.Sum(nil))
}
```

响应格式：

```go
func BuildUpgradeResponse(key string) []byte {
	accept := AcceptKey(key)

	return []byte(fmt.Sprintf(
		"HTTP/1.1 101 Switching Protocols\r\n"+
			"Upgrade: websocket\r\n"+
			"Connection: Upgrade\r\n"+
			"Sec-WebSocket-Accept: %s\r\n\r\n",
		accept,
	))
}
```

生产实现还必须处理 HTTP 请求分片、Header 大小限制、大小写、协议版本、超时和非法请求，不能只用 `bytes.Contains(data, []byte("Upgrade: websocket"))` 判断。

### WebSocket Frame

Upgrade 后的数据是 WebSocket Frame，而不是裸 protobuf：

```text
WebSocket Frame
      |
Frame 解码、反掩码、分片聚合
      |
protobuf payload
```

原示例中的：

```go
func ReadMessage(conn net.Conn) ([]byte, error) {
	return wsutil.ReadClientData(conn)
}
```

不能直接套用到所有 `gnet.Conn` 处理流程；实际接入需要适配 `gnet` 的缓冲区和事件驱动模型，并正确处理半包、粘包、控制帧、分片帧、客户端掩码和最大消息长度。

### protobuf 协议

相较 JSON，protobuf 通常具有更小的消息体和更低的解析开销，但具体大小必须以真实字段和编码结果为准。

`proto/danmaku.proto`：

```protobuf
syntax = "proto3";

package danmaku;

option go_package = "./proto";

message DanmakuMessage {
  uint64 user_id = 1;
  uint64 room_id = 2;
  string content = 3;
  uint64 timestamp = 4;
  MessageType type = 5;
}

enum MessageType {
  TEXT = 0;
  LIKE = 1;
  GIFT = 2;
}
```

生成 Go 代码：

```bash
go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
protoc --go_out=. proto/danmaku.proto
```

编码与解码：

```go
msg := &danmaku.DanmakuMessage{
	UserId:    10001,
	RoomId:    888,
	Content:   "666",
	Timestamp: uint64(time.Now().Unix()),
}

data, err := proto.Marshal(msg)
```

```go
package gateway

import (
	pb "github.com/company/danmaku/proto"
	"google.golang.org/protobuf/proto"
)

func DecodeMessage(data []byte) (*pb.DanmakuMessage, error) {
	msg := &pb.DanmakuMessage{}
	err := proto.Unmarshal(data, msg)
	return msg, err
}
```

### OnTraffic 数据链路

```go
func (s *Server) OnTraffic(c gnet.Conn) error {
	data, err := c.Next(-1)
	if err != nil {
		return err
	}

	// TODO websocket upgrade 与 frame 解析
	payload, err := parseFrame(data)
	if err != nil {
		return err
	}

	msg, err := DecodeMessage(payload)
	if err != nil {
		return err
	}

	room := s.Rooms.Get(int64(msg.RoomId))
	room.Broadcast(data)

	// Kafka 写入需要移出 EventLoop，避免阻塞网络事件处理。
	return nil
}
```

**注意：** `c.Next(-1)` 返回的是当前缓冲区数据，不等于“一次完整 HTTP 请求”或“一条完整 WebSocket 消息”。协议解析器必须支持增量解析。

***
<br/>

> <h3 id="心跳关闭与性能优化">心跳、关闭与性能优化</h3>

### 心跳检测

不能为百万连接分别创建 `time.After()` 或独立 Timer。原始骨架使用全量扫描：

```text
Ticker
  |
每 5 秒扫描
  |
connection.LastActive
```

```go
func Check(manager *connection.Manager) {
	now := time.Now().Unix()

	manager.Range(func(c *connection.Connection) {
		if now-c.LastActive > 60 {
			close(c.Close)
		}
	})
}
```

全量扫描实现简单，但连接规模较大时会形成周期性 CPU 峰值。生产环境可使用分桶扫描或时间轮，例如 `github.com/RussellLuo/timingwheel`。

### 优雅关闭

```text
SIGTERM
  |
停止 accept
  |
标记 Gateway draining
  |
等待写队列和连接退出
  |
关闭 Kafka Producer
  |
退出
```

```go
ctx, cancel := signal.NotifyContext(
	context.Background(),
	os.Interrupt,
	syscall.SIGTERM,
)
defer cancel()

<-ctx.Done()
gateway.Shutdown()
```

实际关闭流程需要设置最大等待时间，避免慢连接或外部依赖导致进程无法退出。

### 百万连接优化原则

**1. 避免每连接一个常驻 goroutine**

```go
go read(conn)
```

百万连接对应百万级 goroutine，会带来栈内存和调度压力。`gnet` 使用 EventLoop 管理大量连接：

```text
CPU Core
  |
EventLoop
  |
Connections
```

**2. 使用对象池时明确所有权**

```go
var pool = sync.Pool{
	New: func() any {
		return make([]byte, 4096)
	},
}
```

对象池可减少部分分配和 GC，但广播数据异步写入时不能提前复用底层字节切片，否则会产生数据竞争或内容污染。

**3. Room 分片**

单个 `sync.Map` 只是骨架。高并发场景可按 `roomID` 将房间分散到多个 shard：

```text
RoomShard[1024]
      |
 roomID hash
      ↓
    shard
      ↓
     room
```

**4. 处理广播放大效应**

房间广播复杂度与本节点订阅人数近似成正比。热门房间需要批量编码、共享只读 payload、分层 fan-out、背压和慢消费者治理。

***
<br/>

> <h3 id="Kafka-与多-Gateway-路由">Kafka 与多 Gateway 路由</h3>

Gateway 不应在网络事件循环中直接写数据库，也不应同步等待 Kafka：

```text
Client
  |
Gateway
  +-----------------+
  |                 |
Local Broadcast   Producer Queue
                    |
               Kafka Worker
                    |
             Danmaku Service
```

### Kafka Producer 骨架

```go
package kafka

import (
	"context"

	"github.com/segmentio/kafka-go"
)

type Producer struct {
	writer *kafka.Writer
}

func NewProducer() *Producer {
	return &Producer{
		writer: &kafka.Writer{
			Addr:     kafka.TCP("localhost:9092"),
			Topic:    "danmaku-message",
			Balancer: &kafka.Hash{},
		},
	}
}

func (p *Producer) Send(ctx context.Context, key, value []byte) error {
	return p.writer.WriteMessages(ctx, kafka.Message{
		Key:   key,
		Value: value,
	})
}

func (p *Producer) Close() error {
	return p.writer.Close()
}
```

若在 `OnTraffic` 中直接调用 `Send()`，网络 EventLoop 可能被 Broker 延迟阻塞。生产链路应使用有界队列和 Producer Worker，并定义队列满、重试、超时和失败降级策略：

```text
Gateway
  |
bounded channel
  |
Kafka Producer Worker
```

### Partition 与房间顺序

同一房间的消息需要进入同一 partition，才能利用 Kafka partition 内有序的特性：

```text
partition = hash(room_id) % partition_count
```

```go
func Partition(roomID int64, partitionCount uint32) int {
	h := fnv.New32a()
	_ = binary.Write(h, binary.LittleEndian, roomID)
	return int(h.Sum32() % partitionCount)
}
```

Producer 也可以将 `roomID` 作为 Key，由 `Hash` Balancer 选择 partition：

```go
err := writer.WriteMessages(ctx, kafka.Message{
	Key:   []byte(strconv.FormatInt(roomID, 10)),
	Value: data,
})
```

```text
partition 1
  room100
  room200

partition 2
  room300
```

Kafka 只保证 partition 内的记录顺序。Consumer 若对同一 partition 再并发处理，仍需自行保证同一房间的处理顺序。

### 多 Gateway 广播

```text
          LB
           |
 +---------+---------+
 |         |         |
GW1       GW2       GW3
```

用户 A 连接 `GW1`、用户 B 连接 `GW3` 时，`GW1` 的本地 Room 无法直接触达 `GW3` 上的连接，因此需要跨节点消息分发。

一种思路是计算 Room Owner：

```text
owner = hash(roomID) % gateway_count

room888
   |
  hash
   ↓
  GW3
```

```go
func Owner(roomID int64) string {
	index := hash(roomID) % len(gateways)
	return gateways[index]
}
```

Gateway 注册信息可存放在 Redis：

```text
gateway:list

gw1 -> 10.0.0.1
gw2 -> 10.0.0.2
gw3 -> 10.0.0.3
```

跨节点广播链路：

```text
Client
  |
GW1
  |
Room Router
  |
Kafka broadcast topic
  |
相关 Gateway Consumer
  |
Local Room
  |
WebSocket Clients
```

> **关键修正：** 若某房间的客户端同时分布在多个 Gateway，仅让单一 Owner Gateway 做本地广播并不能触达其他节点。还需要维护房间到 Gateway 的订阅关系，或让所有订阅该房间的 Gateway 消费对应广播事件。

动态 Gateway 列表直接使用 `hash(roomID) % gateway_count` 会在节点增减时造成大规模重映射，生产环境通常使用一致性哈希、固定虚拟分片或独立路由表。

***
<br/>

> <h3 id="压测与生产参数">压测与生产参数</h3>

### WebSocket 压测

`wrk` 主要面向 HTTP request/response，不适合直接模拟大量长期 WebSocket 连接。可编写 Go 压测客户端：

```go
package main

import (
	"log"
	"net/url"
	"sync"

	"github.com/gorilla/websocket"
)

func main() {
	var wg sync.WaitGroup

	for i := 0; i < 100000; i++ {
		wg.Add(1)

		go func() {
			defer wg.Done()

			u := url.URL{
				Scheme: "ws",
				Host:   "127.0.0.1:9000",
				Path:   "/ws",
			}

			conn, _, err := websocket.DefaultDialer.Dial(u.String(), nil)
			if err != nil {
				log.Printf("dial: %v", err)
				return
			}
			defer conn.Close()

			for {
				if _, _, err = conn.ReadMessage(); err != nil {
					return
				}
			}
		}()
	}

	wg.Wait()
}
```

```bash
go run stress/main.go
```

单台压测机通常会先受本机端口、文件描述符、内存、CPU 和网卡限制，百万连接压测需要多台压测机并校验服务端和客户端两侧瓶颈。

### 关注指标

- **连接：** 当前连接数、建连成功率、断连率、握手延迟。
- **资源：** CPU、RSS、每连接内存、GC、goroutine、文件描述符。
- **消息：** 入站/出站吞吐、广播 fan-out、队列长度、丢弃数。
- **延迟：** P50/P95/P99 端到端延迟。
- **依赖：** Kafka 发送耗时、失败率、重试数和积压量。

示例命令：

```bash
ss -ant | grep ':9000' | wc -l
top
free -m
```

消息中可加入 `send_time`，通过 `receive_time - send_time` 计算端到端延迟。跨机器测试前必须同步时钟。

### Linux 参数

```bash
ulimit -n 2000000
```

```text
net.core.somaxconn=65535
net.ipv4.tcp_tw_reuse=1
```

这些参数不能机械照抄。应结合内核版本、连接模型、端口范围、内存预算、网卡队列和容器限制评估，并同时检查 systemd、容器及进程级文件描述符限制。

***
<br/>

> <h3 id="当前缺口与演进方向">当前缺口与演进方向</h3>

本文代码是**设计骨架和局部示例，不是完整可运行的生产 Gateway**。当前仍缺少：

1. HTTP Upgrade 增量解析、校验、超时和连接状态机。
2. WebSocket Frame 编解码、掩码、分片、Ping/Pong、Close 处理及消息大小限制。
3. `gnet.Conn` 上下文绑定、连接加入/离开房间及断连清理。
4. 每连接有界 `SendQueue`、批量写、背压和慢客户端淘汰。
5. Room Sharding、热门房间分层广播和共享 payload 生命周期管理。
6. Kafka 异步 Worker、批量发送、超时、重试、幂等与降级策略。
7. Redis Gateway 注册发现、健康检查和节点变更处理。
8. 房间订阅路由与多 Gateway 跨节点广播。
9. 鉴权、限流、防重放、内容审核和可观测性。
10. 完整测试，包括协议测试、竞态检测、故障注入和分布式压力测试。

建议按以下顺序演进：

```text
HTTP Upgrade 状态机
        ↓
WebSocket Frame 增量解析
        ↓
连接生命周期与 Room Join/Leave
        ↓
SendQueue + 背压
        ↓
Room Sharding
        ↓
Kafka 异步 Worker
        ↓
Gateway 注册发现与跨节点广播
        ↓
压测、观测与容量规划
```

最终技术选型参考：

| 模块 | 技术 |
| --- | --- |
| 网络层 | `gnet/v2` |
| WebSocket | `gobwas/ws` 或自建增量解析适配层 |
| 协议 | protobuf |
| 连接管理 | EventLoop 本地化、分片容器 |
| 广播 | Room Worker / 分层 fan-out |
| 消息总线 | Kafka |
| 注册与缓存 | Redis Cluster |
| 历史与分析 | ClickHouse |
| 压测 | 自定义 Go WebSocket Client |
| 部署 | Kubernetes |

**核心结论：** Gateway 的难点不是“保持 TCP 连接”本身，而是协议增量解析、广播放大、慢连接背压、跨节点路由、资源上限和故障场景下的稳定性。


***
<br/><br/><br/>
> <h2 id="弹幕V4修改点">弹幕V4修改点</h2>

已根据 /Users/ganghuang/Desktop/Temp.md 对现有弹幕网关和 Kafka 消费链路继续执行生产级优化。本轮重点解决连接存活、跨实例房间订阅正确性和 Kafka 分区阻塞问题。
1. 修改文件
- config/base/app.yaml
- internal/modules/video_danmaku/realtime/hg_gnet_server.go
- internal/modules/video_danmaku/realtime/hg_gnet_server_test.go
- internal/modules/video_danmaku/realtime/hg_room_router.go
- internal/consumer/danmaku/hg_danmaku_consumer.go
- internal/consumer/danmaku/hg_danmaku_consumer_test.go
- internal/pkg/config/hg_env_config.go
- internal/pkg/kafka/hg_consumer.go
- HG_Docuemnts/视频弹幕千万并发与亿级存储部署.md
未修改前端仓库，未覆盖无关文件。
2. 实施内容
WebSocket 应用层心跳
新增配置：
video_danmaku:
  heartbeat_interval: 20s
  heartbeat_timeout: 60s
实现内容：
- 服务端每隔 20s 向已升级连接发送 WebSocket Ping。
- 连接每次收到有效入站流量时更新最后活动时间。
- 连续 60s 没有任何入站帧的连接将被关闭。
- Ping 发送失败的连接也会被关闭。
- 使用单个固定 ticker 扫描所有房间成员。
- 不为每个连接创建 timer。
- 不为每个连接创建心跳 goroutine。
- 关闭连接统一在房间锁释放后执行，避免关闭回调引发锁重入或长时间持锁。
准确的连接容量计数
修复了关闭期间连接计数可能变为负数的问题。
现在每个连接具有独立的 counted 原子状态：
- OnOpen 时只计数一次。
- OnClose 通过 CompareAndSwap 只释放一次。
- 即使连接在网关未就绪、超过连接上限或关闭过程中被拒绝，也不会重复减计数。
Redis 房间订阅状态机
原有逻辑存在以下竞态：
最后一个连接离开
    -> 将 unsubscribe 放入异步队列
    -> 新连接重新加入并订阅
    -> 旧 unsubscribe 延迟执行
    -> 活跃房间被错误退订
现已改成带 generation 的房间订阅状态机：
- 每次订阅状态变化都会推进代次。
- 异步退订命令携带对应代次。
- 执行退订前重新检查：
- 代次是否仍然匹配。
- 本地引用是否仍为零。
- 房间是否处于已订阅状态。
- 旧退订命令不能再移除已经重新加入的房间。
- 退订队列满时保留多余订阅，不阻塞 gnet event-loop。
移除 Redis I/O 全局锁阻塞
原有首连接订阅操作在持有全局 refsMu 时执行 Redis 网络 I/O，可能导致：
- 某个房间订阅变慢。
- 所有其他新房间握手一起等待。
- 重连突刺时 WebSocket 握手尾延迟扩大。
现在：
- Redis Subscribe 不再持有全局引用锁。
- 同一个房间的并发首连接共享一次订阅结果。
- 不同房间可以并行完成订阅。
- 等待订阅结果的连接支持 context 取消。
- Redis 订阅失败不会将房间误标记为成功。
房间路由就绪状态
Redis Pub/Sub 循环不再静默退出：
- 路由器记录运行状态和退出错误。
- Server.Ready() 同时检查 gnet 和房间路由器。
- Pub/Sub 消息通道异常关闭时，网关会转为未就绪。
- Redis 退订失败时，路由器会记录错误并退出。
- Kubernetes readiness probe 可据此摘除失去跨节点广播能力的实例。
Kafka 毒消息处理
弹幕 Kafka Consumer 现在区分两类错误。
不可重试的数据错误：
- JSON 无法解析。
- danmaku_id 为空。
- video_id 为空。
- user_id 为空。
- created_at 无效。
这些错误现在包装为：
HGNewBatchRecordError(
    index,
    HGNewTerminalError(err),
)
结果：
- 精确标记批次中的错误 offset。
- 消息成功进入 DLQ 后可以推进源 offset。
- 单条永久损坏的数据不再无限重试。
- 同一 Kafka partition 后续合法弹幕不会永久被阻塞。
以下基础设施错误仍然保持可重试：
- ClickHouse 写入失败。
- Redis recent 投影失败。
- 网络超时。
- 依赖不可用。
Kafka 错误测试接口
在 internal/pkg/kafka/hg_consumer.go 增加：
HGIsTerminalError(err error) bool
HGBatchFailureIndex(err error, length int) int
用于业务 Consumer 测试终止错误和批次失败位置，不改变现有消费执行语义。
部署文档
部署文档增加心跳约束：
- 默认 Ping 间隔为 20s。
- 默认活动超时为 60s。
- Ingress/LB 空闲超时建议设置为 90s 以上。
- heartbeat_timeout 必须至少是 heartbeat_interval 的两倍。
- 明确心跳扫描不采用每连接 timer/goroutine。
3. 修改原因
本轮处理的是三个可能直接形成生产事故的问题：
1. 没有应用层心跳时，半开连接可能长期占用文件描述符、房间成员、内存和连接额度。
2. 延迟执行的旧退订可能导致活跃网关失去某个房间的跨实例弹幕。
3. 一条永久损坏的 Kafka 消息可能让整个 partition 的 ClickHouse 和 Redis 投影停止推进。
这些问题都无法仅靠增加服务器数量解决，必须在应用层修复正确性和失败处理。
4. 准确性检查
已确认：
- 心跳使用固定 ticker，不产生每连接 timer。
- 心跳扫描不会在房间锁内关闭连接。
- Pong、Ping、文本和其他入站流量都会刷新活动时间。
- 未完成 WebSocket Upgrade 的连接不会进入房间心跳扫描。
- 连接计数通过原子状态保证只释放一次。
- Redis 网络 I/O 不再持有全局房间引用锁。
- 同房间并发首连接不会重复独立维护订阅状态。
- 旧退订代次不能匹配重新加入后的房间。
- 房间仍有本地连接时不会执行退订。
- Pub/Sub 循环异常退出会影响网关 readiness。
- Kafka JSON 和稳定字段错误被标记为终止错误。
- ClickHouse、Redis 等基础设施错误没有被错误标记为终止错误。
- git diff --check 通过。
- 未新增密钥、令牌、连接密码或临时构建产物。
5. 潜在影响
- 网关现在会主动关闭连续 60s 没有入站流量的客户端。
- 标准 WebSocket 客户端通常会自动回复 Pong；自定义客户端必须正确响应服务端 Ping。
- Ingress 或负载均衡器的 WebSocket 空闲超时必须高于应用心跳超时。
- Redis Pub/Sub 循环异常后，网关会变为未就绪，需要 Kubernetes readiness probe 正确配置才能自动摘流。
- Kafka 永久数据错误将进入 DLQ 并推进 offset，生产必须为 DLQ 配置告警和人工处理流程。
- 当前心跳扫描的复杂度仍为每个周期遍历本实例所有房间成员。它避免了百万 timer，但在真实百万连接环境中仍需通过压测确定合理扫描周期和单 Pod 连接上限。
- 本轮没有实现 WebSocket 分片帧重组、完整 Close ACK 等待、Redis recent TTL 或 ClickHouse 显式查询去重，这些仍是后续改进项。
- 当前只能说明完成了高并发架构和故障处理优化，不能声称已经通过百万或千万连接实测。
6. 格式化、编译和测试
已执行 gofmt。
受影响范围测试通过：
go test ./internal/modules/video_danmaku/...
go test ./internal/consumer/danmaku
go test ./internal/infrastructure/kafka
go test ./internal/outbox
go test ./internal/pkg/config
go test ./internal/pkg/clickhouse
go test ./internal/pkg/kafka
go test ./internal/pkg/redis
Race 检查通过：
go test -race ./internal/modules/video_danmaku/realtime
go test -race ./internal/consumer/danmaku
go test -race ./internal/pkg/kafka
静态检查通过：
go vet ./internal/modules/video_danmaku/... \
  ./internal/consumer/danmaku \
  ./internal/infrastructure/kafka \
  ./internal/outbox \
  ./internal/pkg/config \
  ./internal/pkg/clickhouse \
  ./internal/pkg/kafka \
  ./internal/pkg/redis
差异检查通过：
git diff --check
本轮未创建提交，未 push。
7. 后续优化建议
1. 实现 WebSocket Fragment/Continuation 帧的有界重组，兼容完整 RFC 6455 客户端。
2. 将 ACK、Error、Pong、Close 和广播统一纳入每连接出站字节预算。
3. 为连接数、心跳关闭、广播丢弃、慢客户端关闭、Redis 订阅失败和 Kafka DLQ 增加 Prometheus 指标。
4. 为 Redis recent Stream 和 offset Hash 增加同步刷新 TTL，控制长期视频 key 数量。
5. 明确 ClickHouse ReplacingMergeTree 在查询期间的去重策略，避免 Kafka 重放后短期返回重复弹幕。
6. 增加可执行的 WebSocket 压测客户端，分阶段验证 10 万、50 万和 100 万连接。


***
<br/><br/><br/>
># <h1 id="B站级弹幕系统设计V5">B站级弹幕系统设计 V5</h1>

***
<br/><br/><br/>

> <h2 id="Go实时弹幕系统">Go 实时弹幕系统：从 gnet Gateway 到微服务架构</h2>

这套方案使用 `gnet/v2 + gobwas/ws` 构建 WebSocket Gateway，并逐步补齐 Room Sharding、异步消息、心跳、鉴权、限流、存储、服务拆分和 Kubernetes 扩容能力。

整体演进分为三层：

1. **Gateway 核心：**连接状态、WebSocket Frame、本地房间广播和跨节点消息。
2. **生产增强：**EventLoop 写模型、时间轮、JWT、Redis、Kafka、ClickHouse 和 Kubernetes。
3. **微服务落地：**将连接、房间、弹幕业务、存储、在线统计和鉴权拆成独立服务。

```text
                         Client
                           |
                    TCP/WebSocket
                           |
              +-----------------------+
              |     gnet Gateway      |
              |   Connection State    |
              +-----------------------+
                           |
                  WebSocket Decoder
                           |
              +-----------------------+
              |     Room Sharding     |
              |      1024 shards      |
              +-----------------------+
                           |
                   Local Broadcast
                           |
             +-------------+-------------+
             |                           |
        Client SendQueue             Kafka Worker
                                             |
                                    Broadcast Topic
                                             |
                              +--------------+--------------+
                              |                             |
                          Gateway-A                    Gateway-B
                                             |
                                      Redis Registry
```

***
<br/>

> <h2 id="Gateway核心设计">Gateway 核心设计</h2>

## HTTP Upgrade 状态机

WebSocket 连接首先收到 HTTP Upgrade 请求，此时连接还不是 WebSocket：

```http
GET /ws HTTP/1.1
Upgrade: websocket
Sec-WebSocket-Key: xxxx
```

连接状态按以下顺序流转：

```text
TCP CONNECTED
      |
HTTP_HANDSHAKE
      |
WEBSOCKET_READY
```

状态定义：

```go
// connection/state.go
package connection

type State uint8

const (
	StateHandshake State = iota
	StateWebsocket
	StateClosed
)
```

连接对象保存状态、用户、房间和发送队列：

```go
type Connection struct {
	ID uint64

	State State
	Conn  gnet.Conn

	UserID int64
	RoomID int64

	SendQueue chan []byte
}
```

---
<br/>

## HTTP Upgrade 实现

`websocket/upgrade.go`：

```go
package websocket

import (
	"bytes"
	"fmt"
)

func IsWebsocketRequest(data []byte) bool {
	return bytes.Contains(data, []byte("Upgrade: websocket"))
}

func ParseKey(data []byte) string {
	lines := bytes.Split(data, []byte("\r\n"))

	for _, line := range lines {
		if bytes.HasPrefix(line, []byte("Sec-WebSocket-Key")) {
			parts := bytes.Split(line, []byte(":"))
			return string(bytes.TrimSpace(parts[1]))
		}
	}

	return ""
}

func Response(key string) []byte {
	accept := AcceptKey(key)

	return []byte(fmt.Sprintf(
		`HTTP/1.1 101 Switching Protocols\r
Upgrade: websocket\r
Connection: Upgrade\r
Sec-WebSocket-Accept: %s\r
\r
`, accept))
}
```

`ParseKey()` 提取 `Sec-WebSocket-Key`，`Response()` 计算 `Sec-WebSocket-Accept` 并返回 `101 Switching Protocols`。

---
<br/>

## gnet 连接与流量处理

连接建立时创建业务连接对象并加入连接管理器：

```go
func (s *Server) OnOpen(c gnet.Conn) (out []byte, action gnet.Action) {
	client := connection.New(c)
	s.connections.Add(client)
	return
}
```

`OnTraffic()` 根据连接状态分派 HTTP Handshake 或 WebSocket 数据：

```go
func (s *Server) OnTraffic(c gnet.Conn) error {
	client := s.connections.Get(c)

	data, err := c.Next(-1)
	if err != nil {
		return err
	}

	switch client.State {
	case connection.StateHandshake:
		return s.handleHandshake(client, data)

	case connection.StateWebsocket:
		return s.handleWebsocket(client, data)
	}

	return nil
}
```

Handshake 完成后异步写入响应，并将状态切换为 `StateWebsocket`：

```go
func (s *Server) handleHandshake(c *Connection, data []byte) error {
	key := websocket.ParseKey(data)
	if key == "" {
		return nil
	}

	resp := websocket.Response(key)
	c.Conn.AsyncWrite(resp)
	c.State = connection.StateWebsocket

	return nil
}
```

---
<br/>

## WebSocket Frame 编解码

使用：

```text
github.com/gobwas/ws/wsutil
```

`gnet.Conn` 不是 `net.Conn`，原文先给出一个适配器示例：

```go
type GnetConn struct {
	gnet.Conn
}

func (g *GnetConn) Read(p []byte) (int, error) {
	data, err := g.Next(-1)
	copy(p, data)
	return len(data), err
}
```

当前流程已在 `OnTraffic()` 中取得完整字节，因此也可以直接从 `[]byte` 解码客户端 Frame：

```go
func DecodeFrame(data []byte) ([]byte, error) {
	reader := bytes.NewReader(data)
	return wsutil.ReadClientData(reader)
}
```

服务端发送前编码为 WebSocket Text Frame：

```go
func EncodeFrame(data []byte) []byte {
	buf := bytes.Buffer{}
	wsutil.WriteServerMessage(&buf, ws.OpText, data)
	return buf.Bytes()
}
```

---
<br/>

## 每连接 SendQueue

百万连接场景不能简单地为每个连接常驻一个写 goroutine。连接只维护有界发送队列，慢用户队列写满后按策略丢弃消息：

```go
type Connection struct {
	SendQueue chan []byte
}

func New(c gnet.Conn) *Connection {
	return &Connection{
		Conn:      c,
		SendQueue: make(chan []byte, 1024),
	}
}

func (c *Connection) Push(data []byte) {
	select {
	case c.SendQueue <- data:
	default:
		// 慢用户丢弃
	}
}
```

这里的 `SendQueue` 是简化模型，生产版本应由少量 gnet EventLoop 统一刷新，而不是每连接启动一个 goroutine。

---
<br/>

## Room 1024 分片与广播

如果所有房间共用一个容器，千万房间下会形成明显竞争：

```go
sync.Map rooms
```

将房间按 `roomID % 1024` 分片：

```go
const ShardCount = 1024

type RoomManager struct {
	shards [ShardCount]*RoomShard
}

type RoomShard struct {
	rooms sync.Map
}

func shardID(roomID int64) int {
	return int(roomID % ShardCount)
}

func (m *RoomManager) Get(id int64) *Room {
	shard := m.shards[shardID(id)]

	v, _ := shard.rooms.LoadOrStore(id, NewRoom(id))
	return v.(*Room)
}
```

房间广播只遍历本房间连接，并调用 `Push()` 放入发送队列：

```go
func (r *Room) Broadcast(msg []byte) {
	r.clients.Range(func(_, v any) bool {
		client := v.(*Connection)
		client.Push(msg)
		return true
	})
}
```

---
<br/>

## Kafka 异步 Worker

Gateway 的网络事件循环中不能同步执行 `SendKafka()`，因为 Kafka 延迟会阻塞连接处理。使用大容量队列将消息交给异步 Worker：

```go
type Producer struct {
	queue chan *Message
}

func NewProducer() *Producer {
	p := &Producer{
		queue: make(chan *Message, 100000),
	}

	go p.worker()
	return p
}

func (p *Producer) worker() {
	for msg := range p.queue {
		data, _ := proto.Marshal(msg)

		kafka.WriteMessages(
			context.Background(),
			kafka.Message{
				Key: []byte(strconv.FormatInt(msg.RoomID, 10)),
				Value: data,
			},
		)
	}
}
```

Gateway 只执行：

```go
producer.Push(msg)
```

消息进入队列后立即返回，Kafka 写入不再阻塞当前连接事件。

---
<br/>

## Redis Gateway 注册

Redis 保存多个 Gateway 的地址和存活状态，例如：

```text
gateway-1
10.0.0.1:9000

gateway-2
10.0.0.2:9000
```

基础注册：

```go
func Register() {
	redis.Set(
		ctx,
		"gateway:"+id,
		address,
		0,
	)
}
```

生产环境应写入带 TTL 的心跳键，并每 5 秒刷新一次：

```text
SET gateway:gw1 addr EX 10
```

---
<br/>

## 多 Gateway 广播

房间 `888` 的 owner 可按 Gateway 数量计算：

```go
owner = hash(room) % gatewayCount
```

当前 Gateway 不是 owner 时，将消息发送到 Kafka Topic：

```text
room-broadcast
```

消息示例：

```json
{
  "room_id": 888,
  "gateway": "gw3",
  "payload": "hello"
}
```

owner 收到消息后执行：

```go
room.Broadcast()
```

最终数据流：

```text
APP
 |
WebSocket Frame
 |
gnet
 |
Handshake State
 |
ws Decode
 |
protobuf Decode
 |
RoomShard
 |
Room.Broadcast
 |
SendQueue
 |
AsyncWrite

同时：
 |
Kafka Worker
 |
Kafka
 |
其他 Gateway
 |
跨节点广播
```

***
<br/>

> <h2 id="生产能力补齐">生产能力补齐</h2>

生产增强后的整体结构：

```text
                         Client
                           |
                    WebSocket Gateway
                     gnet + gobwas/ws
                           |
        +------------------+------------------+
        |                                     |
   Connection Layer                    Room Engine
        |                                     |
  TimeWheel Heartbeat                 Broadcast
        |
 JWT Auth + Rate Limit
        |
        +------------------+
                           |
                         Kafka
                           |
                  Danmaku Consumer
                           |
                  ClickHouse Storage
                           |
                   Redis Cluster
                           |
                Kubernetes Gateway Pool
```

## 百万连接无 goroutine 写模型

以下写法会导致百万连接对应大量 goroutine，并增加调度器和 GC 压力：

```go
for {
	go conn.Write(msg)
}
```

```text
1,000,000 goroutine
+
scheduler 压力
+
GC 压力
```

gnet Reactor 的核心原则是：**谁负责读，谁负责写。**

```text
CPU Core 0
 EventLoop
   |
 connection1
 connection2

CPU Core 1
 EventLoop
   |
 connection3
```

连接只保存出站缓冲：

```go
type Connection struct {
	ID   uint64
	Conn gnet.Conn

	// 写缓冲
	Outbound chan []byte
}
```

广播不能直接 `client.Conn.Write(data)`，而是将数据推入有界队列：

```go
func (c *Connection) Push(data []byte) {
	select {
	case c.Outbound <- data:
	default:
		// 丢弃慢用户
	}
}
```

由 EventLoop 批量取出消息并异步发送：

```go
func flush(c *Connection) {
	for {
		select {
		case msg := <-c.Outbound:
			c.Conn.AsyncWrite(msg)
		default:
			return
		}
	}
}
```

```text
100 万连接
    ↓
少量 EventLoop
    ↓
异步写
```

---
<br/>

## 千万连接时间轮心跳

不能为每个连接创建一个 `time.Timer`：

```go
for conn {
	time.After()
}
```

千万连接会产生千万个 Timer。Timing Wheel 用固定数量的槽和一个 Ticker 管理连接超时：

```text
                slot
              0 1 2 3
             /       \
      connection  connection
```

数据结构与基础流程：

```go
type Wheel struct {
	slots   []map[uint64]*Connection
	current int
}

func NewWheel() *Wheel {
	return &Wheel{
		slots: make([]map[uint64]*Connection, 360),
	}
}

func (w *Wheel) Add(c *Connection) {
	index := (c.LastActive + 60) % 360
	w.slots[index][c.ID] = c
}

func (w *Wheel) Tick() {
	w.current++
	expired := w.slots[w.current]

	for _, c := range expired {
		if time.Now().Unix()-c.LastActive > 60 {
			c.Conn.Close()
		}
	}
}
```

```text
1000 万连接
一个 Ticker
解决
```

---
<br/>

## JWT 用户鉴权与房间权限

连接流程：

```text
Client
  |
WebSocket Upgrade
  |
JWT Verify
  |
Join Room
```

JWT Payload 示例：

```json
{
  "uid": 10001,
  "exp": 1720000000
}
```

依赖：

```bash
go get github.com/golang-jwt/jwt/v5
```

验证示例：

```go
func VerifyToken(token string) (int64, error) {
	t, err := jwt.Parse(
		token,
		func(t *jwt.Token) (any, error) {
			return []byte(secret), nil
		},
	)
	if err != nil {
		return 0, err
	}

	claims := t.Claims.(jwt.MapClaims)
	return int64(claims["uid"].(float64)), nil
}
```

用户进入房间 `888` 时，还需检查房间权限。Redis Key 示例：

```text
room:permission:888:user:10001
```

也可以使用 MySQL 关系表：

```sql
room_member

user_id
room_id
role
```

---
<br/>

## Redis 在线人数统计

千万用户场景不能频繁执行：

```sql
MySQL count(*)
```

**UV 统计：Redis HyperLogLog**

```text
PFADD room:uv:888 userId
PFCOUNT room:uv:888
```

**在线连接：Redis Bitmap**

```text
room:online:888
```

用户 `uid=10001` 上线：

```go
redis.SetBit(
	ctx,
	"room:online:888",
	10001,
	1,
)
```

离开时执行 `SetBit(..., 0)`，统计时使用：

```text
BITCOUNT
```

---
<br/>

## 弹幕风控限流

普通用户限制为 `5 条/s`，VIP 限制为 `20 条/s`，用于防止用户刷屏和机器人攻击。

原文使用 Redis Lua 实现固定时间窗口计数：

```lua
local key = KEYS[1]
local limit = tonumber(ARGV[1])

local current = redis.call(
    "INCR",
    key
)

if current == 1 then
    redis.call(
        "EXPIRE",
        key,
        1
    )
end

if current > limit then
    return 0
end

return 1
```

Go 调用：

```go
allowed, err := redis.Eval(
	ctx,
	lua,
	[]string{
		"user:rate:10001",
	},
	5,
).Int()

if allowed == 0 {
	return errors.New("rate limited")
}
```

---
<br/>

## Kafka 消费落 ClickHouse

```text
Kafka
  |
Consumer Group
  |
Batch Insert
  |
ClickHouse
```

Kafka Consumer：

```go
reader := kafka.NewReader(
	kafka.ReaderConfig{
		Brokers: []string{
			"localhost:9092",
		},
		Topic:   "danmaku-message",
		GroupID: "danmaku-storage",
	},
)

for {
	msg, err := reader.ReadMessage(ctx)
	if err != nil {
		break
	}

	save(msg.Value)
}
```

ClickHouse 不应逐条 `INSERT`：

```text
insert
insert
insert
```

应聚合后批量写入：

```go
batch, _ := conn.PrepareBatch(
	ctx,
	`
INSERT INTO danmaku
(
    room_id,
    user_id,
    content,
    time
)
VALUES
`,
)

for _, msg := range msgs {
	batch.Append(
		msg.RoomID,
		msg.UserID,
		msg.Content,
		msg.Time,
	)
}

batch.Send()
```

表结构：

```sql
CREATE TABLE danmaku
(
    room_id UInt64,
    user_id UInt64,
    content String,
    create_time DateTime
)
ENGINE = MergeTree
PARTITION BY toYYYYMM(create_time)
ORDER BY
(
    room_id,
    create_time
);
```

---
<br/>

## Kubernetes Gateway 扩容

千万连接必须水平扩展：

```text
                 LoadBalancer
                       |
        +--------------+--------------+
        |              |              |
       GW1            GW2            GW3
```

Gateway 尽量无状态，只保存连接和临时 Room 数据，不长期保存完整业务数据。

Service 示例：

```yaml
apiVersion: v1
kind: Service
metadata:
  name: danmaku-gateway
spec:
  type: LoadBalancer
  selector:
    app: gateway
  ports:
    - port: 9000
```

Deployment 示例：

```yaml
apiVersion: apps/v1
kind: Deployment
spec:
  replicas: 10
  template:
    spec:
      containers:
        - name: gateway
          image: danmaku-gateway:v1
          ports:
            - containerPort: 9000
```

WebSocket 是长连接，负载均衡需保持连接粘性。Nginx 示例：

```nginx
upstream gateway {
    ip_hash;

    server gw1;
    server gw2;
}
```

Kubernetes 可配置：

```yaml
sessionAffinity: ClientIP
```

Gateway 发现仍可使用 Redis：

```text
gateway:list

gw1=10.0.0.1
gw2=10.0.0.2
```

心跳示例：

```text
SET gateway:gw1 online EX 10
```

完整系统结构：

```text
                       APP
                        |
                  WebSocket
                        |
              +-----------------+
              | Gateway Cluster |
              +-----------------+
                        |
                  Room Sharding
                        |
          +-------------+-------------+
          |                           |
     Local Broadcast                Kafka
          |                           |
       Clients                    Consumer
                                      |
                                ClickHouse

Redis:
- 在线人数
- 限流
- Gateway 注册
- 热点弹幕
```

***
<br/>

> <h2 id="Go微服务仓库拆分">Go 微服务仓库拆分</h2>

单体 `danmaku-gateway` 继续扩展后职责过多，应拆分为：

- **Gateway Service：**连接入口。
- **Room Service：**房间状态与广播。
- **Danmaku Service：**弹幕业务处理。
- **Storage Service：**消费消息并落库。
- **Online Service：**在线统计。
- **User/Auth Service：**用户鉴权。
- **API Service：**HTTP 接口。

## Monorepo 目录结构

项目名：

```text
bilibili-realtime-platform
```

```text
bilibili-realtime-platform/
├── go.mod
├── go.work
├── Makefile
├── README.md
│
├── api
│   └── proto
│       ├── gateway.proto
│       ├── room.proto
│       └── danmaku.proto
│
├── cmd
│   ├── gateway
│   │   └── main.go
│   ├── room
│   │   └── main.go
│   ├── danmaku
│   │   └── main.go
│   ├── storage
│   │   └── main.go
│   └── online
│       └── main.go
│
├── internal
│   ├── gateway
│   │   ├── server.go
│   │   ├── connection.go
│   │   ├── websocket.go
│   │   ├── handshake.go
│   │   ├── heartbeat.go
│   │   └── router.go
│   ├── room
│   │   ├── manager.go
│   │   ├── room.go
│   │   ├── shard.go
│   │   └── broadcaster.go
│   ├── danmaku
│   │   ├── handler.go
│   │   ├── processor.go
│   │   └── filter.go
│   ├── storage
│   │   ├── clickhouse.go
│   │   └── repository.go
│   ├── online
│   │   ├── counter.go
│   │   └── redis.go
│   └── auth
│       └── jwt.go
│
├── pkg
│   ├── kafka
│   │   ├── producer.go
│   │   └── consumer.go
│   ├── redis
│   │   └── client.go
│   ├── clickhouse
│   │   └── client.go
│   ├── snowflake
│   │   └── id.go
│   └── limiter
│       └── token_bucket.go
│
├── configs
│   ├── gateway.yaml
│   ├── kafka.yaml
│   ├── redis.yaml
│   └── clickhouse.yaml
│
├── deployments
│   ├── docker
│   └── kubernetes
│       ├── gateway.yaml
│       ├── room.yaml
│       └── storage.yaml
│
└── scripts
    ├── build.sh
    └── migrate.sh
```

---
<br/>

## 服务关系

```text
                     APP
                      |
             WebSocket Connection
                      |
             +-----------------+
             | Gateway Service |
             +-----------------+
                      |
                 gRPC / Kafka
                      |
        +-------------+-------------+
        |                           |
 Room Service               Danmaku Service
        |                           |
   Broadcast                    Business
                                    |
                              Kafka Topic
                                    |
                            Storage Service
                                    |
                             ClickHouse
```

---
<br/>

## Gateway Service

Gateway 只负责连接、WebSocket、心跳和路由，不直接访问 MySQL、ClickHouse，也不承担 Kafka 消费。

`cmd/gateway/main.go`：

```go
package main

func main() {
	server := gateway.NewServer(
		gateway.Config{
			Port: 9000,
		},
	)

	server.Run()
}
```

目录：

```text
gateway/
├── server.go
├── connection.go
├── websocket.go
└── heartbeat.go
```

`server.go`：

```go
type Server struct {
	Engine gnet.Engine

	ConnManager *ConnectionManager
	RoomRouter  *RoomRouter
}
```

```text
TCP
 |
WebSocket
 |
Room Router
```

---
<br/>

## Room Service

Room Service 管理房间、广播和用户进入退出：

```text
room-service
internal/room
```

Room 模型：

```go
type Room struct {
	ID int64

	Clients sync.Map
	Message chan []byte
}
```

房间示例：

```text
视频：BV1xxx
room_id=10001

Room: 10001
 |
 + user1
 |
 + user2
 |
 + user3
```

Gateway 与 Room Service 不应通过进程内方法直接耦合，生产环境使用 gRPC：

```text
Gateway
  |
gRPC
  |
Room Service
```

`room.proto`：

```protobuf
service RoomService {
    rpc JoinRoom(JoinRequest) returns (JoinResponse);
    rpc Broadcast(BroadcastRequest) returns (Empty);
}
```

---
<br/>

## Danmaku Service

Danmaku Service 处理敏感词、等级、禁言和风控，然后发送 Kafka 消息并触发房间广播：

```text
danmaku-service
internal/danmaku
```

```go
func Handle(msg *DanmakuMessage) {
	Filter(msg)
	Kafka.Send(msg)
	Room.Broadcast(msg)
}
```

Kafka Topic 按用途拆分：

| 用途 | Topic / 分区策略 |
| --- | --- |
| 弹幕消息 | `danmaku-message` |
| 弹幕分区 | `hash(room_id) % partition` |
| 房间广播 | `room-broadcast` |
| 用户行为 | `user-action` |

---
<br/>

## Storage Service

Storage Service 专门消费 Kafka 并批量写入 ClickHouse：

```text
storage-service
internal/storage
```

```text
Kafka
  |
Consumer
  |
Batch
  |
ClickHouse
```

```go
func Consume() {
	for {
		msg := Kafka.Read()
		batch.Add(msg)

		if batch.Size() > 1000 {
			ClickHouse.Insert(batch)
		}
	}
}
```

---
<br/>

## Online 与 Auth Service

Online Service 使用 Redis 维护在线人数：

```text
online-service

room_online:{roomID}
```

示例：

```text
room_online:10001
500000
```

Gateway 建立连接时先通过 User/Auth Service 验证 JWT：

```text
JWT
 |
User Service
 |
验证
```

```text
auth-service
```

---
<br/>

## 公共基础包

`pkg/kafka` 为所有服务提供统一 Producer 接口：

```go
type Producer interface {
	Send(topic string, msg []byte) error
}
```

`pkg/redis` 统一处理 Redis Cluster、超时和 Tracing，避免各服务重复维护客户端初始化逻辑。

---
<br/>

## 服务启动与部署

基础设施和服务启动顺序：

```text
1 Redis
2 Kafka
3 ClickHouse

4 Room Service
5 Danmaku Service
6 Storage Service
7 Gateway
```

Kubernetes 副本示例：

```yaml
kind: Deployment
metadata:
  name: danmaku-gateway
spec:
  replicas: 50
```

```yaml
# Room Service
replicas: 20
```

```yaml
# Storage Service
replicas: 10
```

服务间调用推荐 `Kitex + netpoll`，也可以使用 `grpc-go`：

```text
gateway
  |
grpc
  |
room
```

---
<br/>

## 最终微服务拓扑

```text
                         APP
                          |
                    LoadBalancer
                          |
              +--------------------+
              | Gateway Cluster    |
              | gnet               |
              +--------------------+
                          |
                         gRPC
                          |
              +--------------------+
              | Room Cluster       |
              +--------------------+
                          |
                        Kafka
                          |
        +-----------------+----------------+
        |                                  |
 Danmaku Service                  Storage Service
        |                                  |
   Redis Cluster                     ClickHouse
```

---
<br/>

## AI Coding Agent 生成规则

不要只给出“写一个弹幕系统”这种宽泛要求，应明确技术栈、服务边界和非功能要求：

```text
生成一个 Go 微服务仓库：

项目：
bilibili-realtime-platform

技术：
Go 1.24
gnet/v2
gobwas/ws
protobuf
Kafka franz-go
Redis Cluster
ClickHouse
gRPC
Kubernetes

服务：
1 gateway-service
2 room-service
3 danmaku-service
4 storage-service
5 online-service

要求：
百万 WebSocket 连接设计
Room Sharding
Kafka 异步
ClickHouse Batch Insert
JWT 鉴权
Redis Token Bucket
Prometheus metrics
Graceful shutdown
```

***
<br/>

> <h2 id="最终技术栈与规模估算">最终技术栈与规模估算</h2>

## 生产规模估算

| 规模 | 连接数估算 |
| --- | --- |
| 单 Gateway | `10 万 ~ 50 万` |
| 20 台 | `200 万 ~ 1000 万` |
| 100 台 | 千万级 |

实际容量取决于消息频率、包大小、TLS、内核参数、网络带宽、广播模型和硬件配置，必须通过压测确定。

## Go 技术栈

| 模块 | 技术 |
| --- | --- |
| 网络 | `gnet/v2` |
| WebSocket | `gobwas/ws` |
| 协议 | `protobuf` |
| 连接 | `EventLoop` |
| 心跳 | `Timing Wheel` |
| 认证 | `JWT` |
| 限流 | `Redis Lua` |
| 消息 | `Kafka` |
| 缓存 | `Redis Cluster` |
| 存储 | `ClickHouse` |
| RPC | `Kitex/netpoll` 或 `grpc-go` |
| 部署 | `Kubernetes` |

这套架构适用于弹幕、直播聊天室、游戏实时服务器和 IM 群聊等高并发实时消息系统。核心原则是：**Gateway 专注连接，Room 专注广播，业务异步处理，状态外置，存储批量化，服务可水平扩展。**


## <span id="方案实现后的V5目标与结论">方案实现后的V5 目标与结论</span>

V5 在 V4 已有的 gnet Reactor、WebSocket 应用层心跳、动态 Redis 房间订阅、慢客户端隔离、Kafka 毒消息处理和 ClickHouse 异步历史链路基础上，继续优化网关心跳模型。

V4 已经避免“每连接一个 timer 或 goroutine”，但心跳仍由固定 ticker 周期性遍历本实例全部房间成员。单实例连接数增长到数十万或百万后，这种全量扫描会产生：

- 周期性 `O(N)` 连接遍历。
- 所有房间成员分片锁的集中读取。
- 海量 WebSocket Ping 在同一时刻进入 gnet 异步写队列。
- 心跳扫描与热门房间广播争用相同成员锁域。
- 重连风暴后连接心跳相位高度一致，形成周期性 CPU 和网络峰值。

V5 将该模型替换为**固定 ticker 推进的独立分片时间轮**：

```text
V4:

每 20s
   |
   +--> 扫描全部房间
            |
            +--> 扫描全部成员
                     |
                     +--> Ping / Close


V5:

每 1s
   |
   +--> 推进 64 个时间轮分片
            |
            +--> 仅取每个分片当前到期槽
                     |
                     +--> 锁外 Ping / Close
                              |
                              +--> 活跃连接惰性续期
```

V5 的核心收益不是宣称单机已实测支撑千万连接，而是将常规心跳工作量从“周期性扫描本实例所有连接”调整为“每秒仅处理当前到期槽中的连接”，并隔离心跳与房间广播的锁竞争。

千万连接仍必须依赖多 Gateway Pod 水平扩展。单 Pod 安全连接数必须通过真实机器、内核、网卡和客户端压测得到，不能仅根据代码结构推断。

## <span id="V5时间轮架构">V5 时间轮架构</span>

### 总体结构

```text
                         WebSocket Client
                                |
                                v
                      gnet EventLoop / OnTraffic
                                |
                     atomic lastActive.Store()
                                |
                                v
                    不移动时间轮，不获取轮锁


固定 1s ticker
       |
       v
+-------------------------------+
|     Heartbeat Wheel           |
|                               |
|  shard 0  shard 1 ... shard63 |
|     |        |          |     |
|   slots    slots      slots    |
+-------------------------------+
       |
       v
分离当前到期槽，立即释放 shard lock
       |
       +--> 已超时：Close
       |
       +--> Ping 失败：Close
       |
       +--> 活跃：AsyncWrite(Ping) 后重新调度
```

实现文件：

```text
internal/modules/video_danmaku/realtime/hg_heartbeat_wheel.go
internal/modules/video_danmaku/realtime/hg_gnet_server.go
```

时间轮由以下结构组成：

```go
type hgHeartbeatWheelShard struct {
    mu      sync.Mutex
    cursor  uint64
    buckets []map[*hgConnection]struct{}
}

type hgHeartbeatWheel struct {
    tick      time.Duration
    interval  time.Duration
    timeout   time.Duration
    shards    []hgHeartbeatWheelShard
    nextShard atomic.Uint32
    sequence  atomic.Uint64
}
```

设计约束：

- `tick` 固定为 `1s`，不暴露成易误配的部署参数。
- 默认使用 `64` 个独立心跳分片。
- 每个分片有独立 mutex 和 cursor。
- 时间槽切片在初始化时创建，但槽内 map 按需创建。
- 初始化成本与分片数和槽指针数量相关，不与最大连接数相关。
- 每条连接同时最多存在于一个时间槽。
- 时间轮不复用房间 member shard，避免两个不同热点锁域相互耦合。

### 为什么不使用每连接 timer

禁止以下实现：

```go
time.AfterFunc(timeout, func() {
    conn.Close()
})
```

也禁止：

```go
go heartbeatForConnection(conn)
```

百万连接下，每连接 timer 或 goroutine 会放大：

- Go runtime timer heap 压力。
- goroutine 栈和调度成本。
- GC 扫描对象数量。
- 关闭、取消和重连时的生命周期竞态。

V5 始终只使用固定数量的后台 goroutine：现有网关只增加一个时间轮推进 loop，不随连接数增长。

### 为什么入站流量不移动时间轮节点

客户端每次发送 Ping、Pong 或弹幕时，若都执行以下操作：

```text
删除旧时间槽节点
        +
计算新 deadline
        +
插入新时间槽
```

会把 event-loop 上原本极轻的活动更新变成分片锁、map 删除和 map 插入。热门连接或高频 Pong 会持续竞争时间轮锁。

V5 保持入站热路径只有一次原子写：

```go
state.lastActive.Store(time.Now().UnixNano())
```

时间轮节点到期时才读取最新活动时间，并决定关闭或重新安排。这是惰性续期模型，调度成本与心跳检查次数相关，而不是与全部入站帧数量相关。

## <span id="连接生命周期与并发边界">连接生命周期与并发边界</span>

### OnOpen

TCP 连接建立时只执行：

- 创建 `hgConnection`。
- 初始化 `lastActive`。
- 原子增加连接计数。
- 检查 Gateway readiness 和最大连接数。

此时连接尚未完成票据校验和 WebSocket Upgrade，因此不会注册进时间轮。

### Upgrade 成功

连接只有在以下步骤全部成功后才注册心跳：

```text
HTTP Upgrade 请求完整
        |
Origin 校验通过
        |
一次性 ticket 校验通过
        |
Redis 房间订阅成功
        |
gobwas/ws Upgrade 成功
        |
加入本地房间
        |
注册 Heartbeat Wheel
```

失败握手、非法 Origin、无效 ticket 和房间订阅失败连接不会占用时间轮节点。

### OnTraffic

`OnTraffic` 中的活动刷新仍发生在协议处理之前，因此以下有效入站流量都会刷新活动时间：

- WebSocket Ping。
- WebSocket Pong。
- 文本弹幕命令。
- Close 帧。
- 完整到达后被协议逻辑处理的其他帧。

V5 没有擅自改成“只有 Pong 才刷新”，以保持 V4 的连接存活语义兼容。

### OnClose

关闭顺序为：

```text
CAS 释放连接计数
        |
heartbeatClosed = true
        |
从时间轮槽中删除节点
        |
离开本地房间
        |
释放 Redis 房间订阅引用
```

`heartbeatClosed` 必须在删除时间轮节点前生效，因为时间轮线程可能已经把节点从槽内取到本地切片。即使节点已经不在 bucket 中，关闭标志仍可阻止其处理完成后重新插入。

### gnet 并发安全边界

gnet 明确规定 `Conn.Context()` 不能在 EventHandler 外并发调用。因此时间轮不会在后台线程执行：

```go
state := conn.Context().(*hgConnection)
```

Upgrade 成功时直接保存：

```go
state.heartbeatConn = conn
```

后台心跳只使用 gnet 并发安全的操作：

- `Conn.AsyncWrite`。
- `Conn.Close`。

不在后台线程使用：

- `Conn.Context`。
- `Conn.SetContext`。
- `Conn.Peek`、`Conn.Next` 等输入缓冲 API。
- 非并发安全的同步 `Conn.Write`。

### 锁边界

时间轮 shard lock 内只做：

- 推进 cursor。
- 获取当前 bucket。
- 清空当前 bucket 指针。
- 修改 `heartbeatScheduled`。
- 安排连接到未来 bucket。

时间轮锁内禁止：

- `AsyncWrite`。
- `Close`。
- 房间 Join/Leave。
- Redis、MySQL、Kafka 或 ClickHouse I/O。
- 获取 room directory/member lock。

因此时间轮锁和房间广播锁之间不存在嵌套锁顺序。

## <span id="时间轮调度算法">时间轮调度算法</span>

### 首次调度相位打散

滚动发布、机房网络恢复或 Ingress 切流后，可能在短时间建立大量连接。如果所有连接统一安排为 `now + 20s`，即使采用时间轮，仍会在同一个槽产生 Ping 峰值。

V5 使用原子 sequence 将首次调度分散到一个 heartbeat interval 内：

```go
intervalTicks := hgHeartbeatDurationTicks(w.interval, w.tick)
delayTicks := uint64(1)
if intervalTicks > 1 {
    delayTicks += (w.sequence.Add(1) - 1) % uint64(intervalTicks)
}
```

默认配置下，首个 Ping 被分散到连接建立后的约 `1s-20s`。后续检查仍以 `heartbeat_interval` 为上限。

### 分片选择

连接使用独立的全局轮询计数分配到心跳 shard：

```go
state.heartbeatShard =
    (w.nextShard.Add(1) - 1) & uint32(len(w.shards)-1)
```

由于 `heartbeat_shard_count` 必须为 2 的幂，可使用位掩码代替取模。独立轮询避免复用 `memberShard` 时长尾房间首连接集中到相同心跳分片。

### Tick 推进

每秒推进一次全部 shard，但每个 shard 只取当前槽：

```go
shard.cursor++
slot := int(shard.cursor % uint64(len(shard.buckets)))
bucket := shard.buckets[slot]
shard.buckets[slot] = nil
```

随后立刻释放 shard lock，再处理 bucket 中的连接。

### 超时判断

```go
deadline := now.Add(-heartbeatTimeout).UnixNano()

if state.lastActive.Load() <= deadline {
    conn.Close()
}
```

默认 `heartbeat_timeout = 60s`。连接连续 60 秒没有任何有效入站活动时关闭。

### Ping 与续期

活跃连接执行：

```go
if err := conn.AsyncWrite(heartbeatPing, nil); err != nil {
    conn.Close()
    return
}

heartbeatWheel.hgReschedule(state, now)
```

`AsyncWrite` 成功只代表帧进入 gnet 异步写路径，不代表客户端已经收到或已经回复。因此发送 Ping 不会更新 `lastActive`。只有客户端后续 Pong 或其他有效入站流量才刷新活动时间。

下一次检查延迟取：

```text
min(heartbeat_interval, last_active + heartbeat_timeout - now)
```

这保证：

- 活跃连接不会比配置间隔更频繁地收到 Ping。
- 静默连接会在 timeout 附近再次被检查。
- 无需每次入站流量移动时间轮节点。

### 取消与重新调度竞态

重点防护以下时序：

```text
Tick 从 bucket 取出连接
        |
OnClose 标记连接关闭
        |
Tick 完成 Ping
        |
尝试重新插入未来 bucket
```

V5 使用双重检查：

1. `hgReschedule` 前检查 `heartbeatClosed`。
2. 获取 shard lock 后再次检查 `heartbeatClosed`。

因此关闭连接不会重新挂回时间轮，也不会形成长期内存滞留。

## <span id="配置与容量边界">配置与容量边界</span>

默认配置：

```yaml
video_danmaku:
  heartbeat_interval: 20s
  heartbeat_timeout: 60s
  heartbeat_shard_count: 64
```

约束：

| 配置 | 默认值 | 约束 | 说明 |
| --- | ---: | --- | --- |
| `heartbeat_interval` | `20s` | `5s-1m` | 活跃连接 Ping 周期上限 |
| `heartbeat_timeout` | `60s` | 至少为 interval 的 2 倍，最大 `5m` | 静默连接清理时间 |
| `heartbeat_shard_count` | `64` | `4-256` 且为 2 的幂 | 时间轮独立锁分片数量 |
| 内部 wheel tick | `1s` | 固定 | 时间轮推进精度，不暴露为配置 |

Ingress/LB 空闲超时必须高于应用心跳超时，默认建议至少 `90s`。

### 复杂度

V4 常规心跳扫描：

```text
每个 heartbeat interval: O(本实例全部连接)
```

V5 常规时间轮推进：

```text
每个 1s tick: O(heartbeat shard 数 + 当前到期连接数)
```

需要注意，连接最终仍会按配置周期执行 Ping，因此整体长期 Ping 数量仍与连接数成正比。时间轮消除的是集中全量扫描和锁遍历，不可能消除维持 WebSocket 存活所需的网络工作。

### 内存边界

每条已升级连接新增保存：

- `heartbeatConn` 接口引用。
- shard 和 slot 索引。
- scheduled 状态。
- closed 原子状态。
- bucket map 中的一个连接指针 key。

时间槽 map 按需创建，避免为所有 `shard × slot` 预创建空 map。但实际每连接内存仍必须通过真实构建、Go 版本和目标架构下的 RSS 压测测量，不能仅按字段大小相加得出生产容量。

### 千万连接部署结论

V5 仅是单 Gateway 节点的心跳调度优化。千万连接部署仍遵循：

```text
required_pods =
    ceil(target_connections / verified_connections_per_pod)
    * safety_factor
```

安全系数建议不低于 `1.3`，并预留：

- 滚动发布容量。
- 单 AZ 故障容量。
- 热门视频倾斜。
- Redis/Kafka 故障期间的降级余量。
- 网卡和 CPU 突刺余量。

## <span id="V5准确性与验证">V5 准确性与验证</span>

### 单元测试

新增文件：

```text
internal/modules/video_danmaku/realtime/hg_heartbeat_wheel_test.go
```

覆盖：

- 初始连接在 shard 间均匀轮询分配。
- 首次调度在多个时间槽间分散。
- 取消后连接不会进入到期结果。
- 重复取消保持幂等。
- 已关闭连接不能重新调度。
- 活跃连接可以重新进入未来时间槽。
- `Cancel` 和 `Reschedule` 并发执行不会留下已关闭节点。
- 非 2 的幂 `heartbeat_shard_count` 被配置校验拒绝。
- interval、timeout 和 shard 默认配置正确解析。

### 已执行验证

格式化：

```bash
gofmt -w \
  internal/modules/video_danmaku/realtime/hg_gnet_server.go \
  internal/modules/video_danmaku/realtime/hg_heartbeat_wheel.go \
  internal/modules/video_danmaku/realtime/hg_heartbeat_wheel_test.go \
  internal/pkg/config/hg_env_config.go \
  internal/pkg/config/hg_video_danmaku_config_test.go
```

业务测试：

```bash
go test ./internal/modules/video_danmaku/... \
  ./internal/pkg/config \
  ./internal/pkg/redis
```

Race 检查：

```bash
go test -race -count=20 \
  ./internal/modules/video_danmaku/realtime
```

静态检查：

```bash
go vet ./internal/modules/video_danmaku/... \
  ./internal/pkg/config \
  ./internal/pkg/redis
```

差异检查：

```bash
git diff --check
git diff --cached --check
git diff HEAD^ HEAD --check
```

以上验证均已通过。

### 10 万连接状态基准

测试环境：

```text
darwin/arm64
Apple M2 Pro
```

执行命令：

```bash
go test -run '^$' \
  -bench 'BenchmarkHeartbeatWheelScheduleAndAdvance100K' \
  -benchtime=1x \
  -benchmem \
  ./internal/modules/video_danmaku/realtime
```

结果：

```text
BenchmarkHeartbeatWheelScheduleAndAdvance100K-10
1 iteration
8,059,125 ns/op
23,626,176 B/op
5,127 allocs/op
```

该基准包括：

- 创建 10 万个测试连接状态。
- 注册到 64 个时间轮分片。
- 推进 20 个时间槽。

结果中的内存包含测试一次性创建的 10 万连接状态数组和 bucket 容器，不等同于线上每 WebSocket 连接内存。该基准也不包含 socket、TLS、内核网络栈、gnet 缓冲、WebSocket 帧编解码和真实客户端 Pong。

### 未验证边界

V5 尚未执行：

- 10 万真实 WebSocket 长连接压测。
- 50 万真实 WebSocket 长连接压测。
- 100 万真实 WebSocket 长连接压测。
- 多压测机千万连接集群测试。
- Linux 生产内核下的 RSS、GC、event-loop CPU 和 NIC 带宽验证。
- Redis Cluster、Kafka、ClickHouse 故障注入与心跳压力叠加测试。

因此准确结论是：**V5 已完成面向百万连接单节点模型和千万连接集群模型的时间轮设计优化，并通过单元、race、vet 和 10 万状态调度基准；未实测证明当前部署已经支撑百万或千万真实连接。**

## <span id="弹幕V5修改点">弹幕V5修改点</span>

### 修改文件

```text
HG_Docuemnts/视频弹幕千万并发与亿级存储部署.md
config/base/app.yaml
internal/modules/video_danmaku/realtime/hg_gnet_server.go
internal/modules/video_danmaku/realtime/hg_heartbeat_wheel.go
internal/modules/video_danmaku/realtime/hg_heartbeat_wheel_test.go
internal/pkg/config/hg_env_config.go
internal/pkg/config/hg_video_danmaku_config_test.go
```

### 功能变化

1. 将 V4 的固定周期全房间成员扫描替换为固定 `1s` ticker 推进的分片时间轮。
2. 新增独立 `heartbeat_shard_count`，默认 `64`。
3. 首次心跳在一个 interval 内打散，缓解重连风暴后的 Ping 峰值。
4. 入站流量只执行原子 `lastActive` 更新，不移动时间轮节点。
5. 时间轮锁内不执行网络写、关闭或房间操作。
6. 使用 `heartbeatClosed` 解决 OnClose 与重新调度竞态。
7. WebSocket Upgrade 成功后注册，OnClose 时幂等取消。
8. 时间轮 panic 时撤销 Gateway readiness。
9. 增加时间轮调度、取消、竞态和配置测试。
10. 增加 10 万连接状态注册和槽推进 benchmark。

### 兼容性

- WebSocket 路径不变。
- ticket 鉴权语义不变。
- JSON 命令、ACK 和 Error 结构不变。
- Ping 间隔和活动超时默认值不变。
- Redis 房间频道不变。
- Kafka Topic 和 ClickHouse 表结构不变。
- MySQL 幂等和 Outbox 事务语义不变。
- 首个 Ping 从统一等待完整 interval 改为在一个 interval 内分散发送。

### 提交信息

```text
a41ed49 feat: 🫧 弹幕网关新增分片时间轮心跳
```

该提交已创建在本地 `develop` 分支，未执行 push。

### 下一阶段建议

按收益排序：

1. 为时间轮增加注册连接数、bucket 深度、tick 耗时、超时关闭和 Ping 写失败 Prometheus 指标。
2. 开发基于 gnet 或 netpoll 的多机 WebSocket 压测客户端，分阶段验证 10 万、50 万和 100 万连接。
3. 将 ACK、Error、Pong、Close 和房间广播统一纳入每连接出站字节预算。
4. 实现 WebSocket Fragment/Continuation 帧有界重组和完整 Close 握手。
5. 为 Redis recent Stream 和 offset 水位增加一致 TTL，限制亿级历史下的长期 key 数量。
6. 完成 ClickHouse 历史读取灰度、重放去重验证和冷热查询路由。
