- [B 站级弹幕系统设计 V1](#B站级弹幕系统设计V1)
	- [Redis 的用途](#Redis的用途)
	- [亿级弹幕存储](#亿级弹幕存储)
	- [网络模型](#网络模型)
	- [千万连接的机器规模](#千万连接的机器规模)
	- [推荐方案](#推荐方案)
	- [Go 工程设计](#Go工程设计)
- [**‌ B 站级弹幕系统设计 V2**](#B站级弹幕系统设计V2)
	- [gobwas/ws 与 gnet 构建高性能 WebSocket 网关](#gobwasws-与-gnet-构建高性能-websocket-网关)
	- [gobwas/ws 的定位](#gobwasws-的定位)
	- [gobwas/ws 与 gorilla/websocket](#gobwasws-与-gorillawebsocket)
	- [gnet 与 gobwas/ws 的分工](#gnet-与-gobwasws-的分工)
	- [Go 工程接入](#go-工程接入)
	- [连接与广播设计](#连接与广播设计)
	- [技术选型](#技术选型)
	- [弹幕 V2 修改点](#弹幕V2修改点)
- [B站级弹幕系统设计 V3](#B站级弹幕系统设计V3)
	- [Go 实时消息平台架构设计](#Go实时消息平台架构设计)
	- [整体架构](#整体架构)
	- [工程与服务拆分](#工程与服务拆分)
	- [Gateway 与连接模型](#Gateway与连接模型)
	- [Room Manager 与广播模型](#RoomManager与广播模型)
	- [消息协议与发送流程](#消息协议与发送流程)
	- [Kafka、Redis 与 ClickHouse](#KafkaRedis与ClickHouse)
	- [限流与可观测性](#限流与可观测性)
	- [AI Coding Agent 提示词](#AICodingAgent提示词)
	- [生产级技术选型](#生产级技术选型)
	- [最终架构](#最终架构)
	- [弹幕V3修改点](#弹幕V3修改点)

---

<br/><br/><br/>

> <h2 id="B站级弹幕系统设计V1">B站级弹幕系统设计V1</h2>

目标：使用 Go 实现仿 B 站、斗鱼、虎牙的弹幕系统，支撑**千万并发连接和亿级弹幕数据**。

核心不在于单个 WebSocket 库，而在于：**连接模型、房间广播模型、消息削峰、冷热数据分离**。

---

<br/>

> <h3 id="Redis的用途">Redis 的用途</h3>

Redis 不保存全部弹幕，主要用于热门房间状态和最近弹幕等热数据。

## 热门房间状态

例如，使用 `room:10001` 保存房间在线人数和最后消费位置：

```text
room:10001

{
 online:5000000,
 last_cursor:123456
}
```

## 最近弹幕

可使用 `ZSET` 保存最近一段时间的弹幕：

```text
danmaku:10001

score=time
value=json
```

写入示例：

```text
ZADD danmaku:10001 1720000000 msg
```

只保留最近 5 分钟的数据，历史弹幕交给持久化存储。

---

<br/>

> <h3 id="亿级弹幕存储">亿级弹幕存储</h3>

不要使用 MySQL 单表保存十亿级弹幕。例如：

```text
danmaku

id
danmu_id // 字符串
video_id
user_id
content

10亿行
```

单表达到该规模后，查询和维护成本会非常高。推荐进行冷热数据分离：

- **热数据：** Redis。
- **历史数据：** ClickHouse。

ClickHouse 历史表：

```text
danmaku_history
```

主要字段：

```text
video_id
user_id
content
create_time
```

按月份分区：

```sql
PARTITION BY month(create_time)
```

按视频和创建时间排序：

```sql
ORDER BY
(
 video_id,
 create_time
)
```

该结构适合查询视频回放弹幕。

---

<br/>

> <h3 id="网络模型">网络模型</h3>

B 站级弹幕系统可采用以下网络与消息链路：

```text
Linux

epoll
 |
gnet
 |
gobwas/ws
 |
Room Engine
 |
Kafka
```

Go WebSocket 接入层选择：

```text
gnet + gobwas/ws
```

- `gnet` 提供基于 Reactor 模型的高性能事件驱动网络能力。
- `gobwas/ws` 负责 WebSocket 协议处理。
- `Room Engine` 负责房间管理和广播。
- `Kafka` 负责消息削峰和异步解耦。

---

<br/>

> <h3 id="千万连接的机器规模">千万连接的机器规模</h3>

单机经过系统参数和程序优化后，可承载约 `10万~50万` WebSocket 连接，实际容量取决于消息频率、内存占用、广播模型和硬件配置。

千万连接需要 Gateway 集群。例如使用 100 台 Gateway，每台承载 10 万连接：

```text
10,000,000 users

        |

100 gateway

每台:
100,000 connections
```

---

<br/>

> <h3 id="推荐方案">推荐方案</h3>

| 模块         | 技术选型           |
| ------------ | ------------------ |
| WebSocket 层 | `gobwas/ws`        |
| 网络模型     | `gnet`             |
| 消息队列     | `Kafka`            |
| 热状态       | `Redis Cluster`    |
| 历史弹幕     | `ClickHouse`       |
| 服务发现     | `Nacos` / `Consul` |
| RPC          | `gRPC`             |

完整架构：

```text
                 User

                  |
              WebSocket
                  |
          +---------------+
          | Danmaku GW    |
          +---------------+

                  |
              Kafka
                  |
        +----------------+
        | Room Service   |
        +----------------+

          |          |

       Redis     ClickHouse
```

对于“B 站级弹幕、千万并发、亿级数据、Go 工程”这一目标，推荐选择 **`gnet + gobwas/ws`**。

---

<br/>

> <h3 id="Go工程设计">Go 工程设计</h3>

## 弹幕入口

```text
gnet
+
gobwas/ws
```

主要负责：

- TCP 网络通信。
- WebSocket 协议处理。
- 心跳检测。
- 长连接管理。

## 内部 RPC

```text
Kitex
+
netpoll
```

主要用于服务间调用，包括用户服务、视频服务和推荐服务。

## 消息队列

```text
Kafka
```

Kafka 用于消息削峰、异步处理和服务解耦。

## 存储

- **实时热数据：** `Redis Cluster`。
- **历史弹幕：** `ClickHouse`。

## 最终架构

```text
                 Client

                   |
              WebSocket
                   |
        +----------------+
        | gnet Gateway  |
        +----------------+

                   |
              Kafka
                   |
        +----------------+
        | Room Service  |
        +----------------+

          |             |

       Redis       ClickHouse


内部服务：

Kitex
 |
netpoll
```

最终选择：`gnet + gobwas/ws`。

主要原因：

- 采用 Reactor 模型。
- 编程模型接近 Netty。
- 长连接承载能力强。
- 适合房间广播场景。
- 对网络层的控制粒度高。

**最终结论：WebSocket 库只是接入层的一部分，系统能否支撑千万并发，取决于连接承载、房间广播、消息削峰以及冷热数据分离的整体设计。**

<br/>

---

<br/><br/><br/>

> # <h1 id="B站级弹幕系统设计V2">B 站级弹幕系统设计 V2</h1>

---

<br/><br/><br/>

> <h2 id="gobwasws-与-gnet-构建高性能-websocket-网关">gobwas/ws 与 gnet 构建高性能 WebSocket 网关</h2>

`gobwas/ws` 是 Go 的高性能 WebSocket 协议库。它不像 `gorilla/websocket` 那样强调完整、易用的连接封装，而是提供更接近底层 TCP 的协议升级、帧解析和读写能力。

在弹幕、直播间聊天和大规模长连接网关中，各层职责通常是：

- **`gnet`：**负责高性能网络事件循环和 TCP 连接管理。
- **`gobwas/ws`：**负责 WebSocket Upgrade、Frame、Mask、Ping/Pong 和 Close 等协议处理。
- **业务层：**负责用户、房间、消息路由、弹幕广播和数据存储。

```text
TCP 连接
   |
WebSocket 协议解析
   |
业务消息
```

完整链路示意：

```text
                 APP
                  |
             WebSocket
                  |
        +----------------+
        | Load Balancer  |
        +----------------+
                  |
        +----------------+
        | gnet Gateway   |
        +----------------+
                  |
             gobwas/ws
                  |
            Room Manager
                  |
               Kafka
                  |
          Danmaku Service
                  |
       Redis + ClickHouse
```

---

<br/>

> <h3 id="gobwasws-的定位">gobwas/ws 的定位</h3>

## WebSocket 协议流程

客户端首先发送 HTTP Upgrade 请求：

```http
GET /chat HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: xxx
```

服务器返回 `101 Switching Protocols`：

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: xxx
```

升级完成后，连接不再传输普通 HTTP 消息，而是在 TCP 长连接上持续传输 WebSocket Frame：

```text
TCP 长连接

client
  |
server

frame frame frame
```

例如一条弹幕：

```json
{
  "userid": 10001,
  "text": "666"
}
```

实际会封装为：

```text
WebSocket Frame

+---------+
| header  |
+---------+
| payload |
+---------+
```

`gobwas/ws` 主要处理：

- HTTP Upgrade。
- WebSocket Frame 编解码。
- 客户端 Mask 解码。
- Ping/Pong 心跳帧。
- Close 控制帧。

仓库中的常用模块：

```text
gobwas/ws
├── ws
│   └── Upgrade
├── wsutil
│   ├── ReadClientData
│   └── WriteServerMessage
└── wsflate
    └── compression
```

## 性能特点

在百万级连接和高频消息场景中，关键目标是**减少内存分配、数据复制和 GC 压力**。传统处理过程可能经过多次复制：

```text
socket buffer
    ↓ copy
byte[]
    ↓ copy
message
```

底层协议库允许应用更直接地管理缓冲区和解析过程：

```text
socket buffer
      |
   直接解析
```

这不代表所有操作都会自动“零拷贝”，而是 `gobwas/ws` 提供了更低层的 API，便于调用方减少不必要的分配和复制。

---

<br/>

> <h3 id="gobwasws-与-gorillawebsocket">gobwas/ws 与 gorilla/websocket</h3>

| 对比项   | `gorilla/websocket`         | `gobwas/ws`                        |
| -------- | --------------------------- | ---------------------------------- |
| 定位     | 完整、易用的 WebSocket 封装 | 偏底层的协议工具库                 |
| 使用成本 | 较低                        | 较高                               |
| 控制能力 | 常规连接读写                | 可细粒度控制读写、缓冲区和并发模型 |
| 适用场景 | 中小型服务、快速开发        | 自定义网络栈、高性能网关           |

`gorilla/websocket` 的典型代码：

```go
conn, err := websocket.Upgrade(
	w,
	r,
	nil,
	1024,
	1024,
)
if err != nil {
	return
}

for {
	_, msg, err := conn.ReadMessage()
	if err != nil {
		return
	}
	_ = msg
}
```

常见连接模型：

```text
一个连接
   |
goroutine
   |
read loop
   |
write loop
```

该模式简单稳定，但如果每个连接都绑定独立 goroutine 和较大的缓冲区，百万连接下会带来明显的调度与内存压力。具体压力取决于实现方式，并非 `gorilla/websocket` 强制要求固定的“一连接一 goroutine”。

`gobwas/ws` 的常见使用方式：

```go
conn, _, _, err := ws.UpgradeHTTP(r, w)
if err != nil {
	return
}

msg, op, err := wsutil.ReadClientData(conn)
if err != nil {
	return
}

_ = msg
_ = op
```

应用可以自行控制：

```text
什么时候读
什么时候写
buffer 怎么管理
goroutine 怎么分配
```

**选择原则：**优先根据连接规模、团队维护成本和自定义网络模型决定，不要仅因“性能更高”就直接选择更底层的库。

---

<br/>

> <h3 id="gnet-与-gobwasws-的分工">gnet 与 gobwas/ws 的分工</h3>

`gnet` 处理 TCP 事件，但不会自动理解 WebSocket Frame。例如收到以下字节：

```text
81 85 37 fa 21 ...
```

`gobwas/ws` 可以将其解析为：

```text
Frame
opcode = text
payload = "hello"
```

组合关系：

```text
                 Client
                   |
              WebSocket
                   |
                  TCP
                   |
                 gnet
                   |
              gobwas/ws
                   |
             Danmaku Logic
```

这类似 Java 网络栈中的：

```text
Netty
  +
WebSocket Codec
```

## 不要直接混用 `gnet` 与 `ws.UpgradeHTTP`

`ws.UpgradeHTTP()` 面向 `net/http` 的 `http.Request`、`http.ResponseWriter` 和劫持后的 `net.Conn`；`gnet.Conn` 则属于事件驱动连接抽象，二者不能仅靠类型转换直接拼接。

下面的代码只能表达意图，不是完整可运行实现：

```go
// 示意代码：gnet 不是 net/http，不能直接这样调用。
func Upgrade(raw net.Conn) {
	conn, _, _, err := ws.UpgradeHTTP(request, response)
	_ = raw
	_ = conn
	_ = err
}
```

在 `gnet` 中通常需要维护连接状态，并在事件回调里完成以下流程：

```text
TCP 连接建立
      |
读取并缓存 HTTP Header
      |
验证 Upgrade: websocket
      |
计算并返回 101 响应
      |
连接切换为 WebSocket 模式
      |
增量解析 WebSocket Frame
```

`wsutil.ReadClientData()` 适合阻塞式 `io.Reader` 场景；在 `gnet` 的事件循环中直接调用阻塞式读取会破坏事件驱动模型。生产实现应使用适合增量数据的握手与 Frame 解析方式，或选择已经完成 `gnet` 集成的 WebSocket Codec。

---

<br/>

> <h3 id="go-工程接入">Go 工程接入</h3>

## 工程结构

```text
danmaku-server/
├── cmd/
│   └── gateway/
│       └── main.go
├── internal/
│   ├── websocket/
│   │   └── server.go
│   ├── room/
│   │   ├── room.go
│   │   └── manager.go
│   └── protocol/
│       └── message.go
├── go.mod
└── go.sum
```

初始化工程：

```bash
mkdir danmaku-server
cd danmaku-server
go mod init github.com/company/danmaku-server
```

安装依赖：

```bash
go get github.com/panjf2000/gnet/v2
go get github.com/gobwas/ws
```

使用 `gnet/v2`，不要引用旧模块路径 `github.com/panjf2000/gnet`。`wsutil` 已包含在 `github.com/gobwas/ws/wsutil` 中，无需单独安装。

## 最小 gnet TCP 服务器

```go
package main

import (
	"log"

	"github.com/panjf2000/gnet/v2"
)

type DanmakuServer struct {
	gnet.BuiltinEventEngine
}

func (s *DanmakuServer) OnBoot(eng gnet.Engine) gnet.Action {
	log.Println("server started")
	return gnet.None
}

func (s *DanmakuServer) OnTraffic(c gnet.Conn) gnet.Action {
	buf, err := c.Next(-1)
	if err != nil {
		return gnet.Close
	}

	log.Println(string(buf))
	return gnet.None
}

func main() {
	server := &DanmakuServer{}

	err := gnet.Run(
		server,
		"tcp://:9000",
		gnet.WithMulticore(true),
	)
	if err != nil {
		panic(err)
	}
}
```

运行：

```bash
go run .
```

此时启动的是事件驱动 TCP 服务：

```text
TCP server :9000
```

注意：这段代码尚未实现 HTTP Upgrade 和 WebSocket Frame 解析。

## 独立 `net/http` Echo 示例

如果不使用 `gnet`，可通过 `net/http + gobwas/ws` 快速构建 WebSocket Echo 服务：

```go
package main

import (
	"net/http"

	"github.com/gobwas/ws"
	"github.com/gobwas/ws/wsutil"
)

func handler(w http.ResponseWriter, r *http.Request) {
	conn, _, _, err := ws.UpgradeHTTP(r, w)
	if err != nil {
		return
	}
	defer conn.Close()

	for {
		msg, err := wsutil.ReadClientText(conn)
		if err != nil {
			return
		}

		println(string(msg))

		if err := wsutil.WriteServerText(conn, msg); err != nil {
			return
		}
	}
}

func main() {
	http.HandleFunc("/ws", handler)
	if err := http.ListenAndServe(":8080", nil); err != nil {
		panic(err)
	}
}
```

客户端发送：

```text
hello
```

服务器返回：

```text
hello
```

---

<br/>

> <h3 id="连接与广播设计">连接与广播设计</h3>

## 连接与房间模型

大规模长连接系统不应只依赖一个无分片的全局 `map[string]*websocket.Conn`。连接对象通常至少记录业务标识、底层连接、房间和待发送队列：

```go
type Client struct {
	ID        int64
	Conn      gnet.Conn
	RoomID    int64
	SendQueue chan []byte
}
```

房间模型示意：

```go
type Room struct {
	ID      int64
	Clients sync.Map
}
```

```text
Room: 10001
  |
  +-- Client
  |
  +-- Client
  |
  +-- Client
```

`sync.Map` 只是示例，不一定是高并发房间管理的最优方案。生产环境通常还需要分片、事件循环归属、背压、慢连接淘汰和连接生命周期管理。

## 弹幕消息模型

不要只传递无结构字符串，应定义明确的协议模型：

```go
type Danmaku struct {
	UserID  int64  `json:"uid"`
	RoomID  int64  `json:"room_id"`
	Content string `json:"content"`
	Time    int64  `json:"time"`
}
```

消息示例：

```json
{
  "uid": 10001,
  "room_id": 888,
  "content": "666"
}
```

## 广播模型

直接同步遍历所有连接并写入消息：

```go
for _, client := range clients {
	client.Write(msg)
}
```

在超大房间中容易被慢连接拖住，也缺少背压与批处理机制。更合理的方向是将接收、路由和广播解耦：

```text
Receive
   |
 Kafka
   |
Room Worker
   |
Batch Broadcast
```

房间广播队列示意：

```go
type Room struct {
	Broadcast chan []byte
}
```

```go
func broadcast(room *Room) {
	for msg := range room.Broadcast {
		room.Clients.Range(func(k, v any) bool {
			client := v.(*Client)
			client.Send(msg)
			return true
		})
	}
}
```

该代码只展示队列化思路。生产实现还需处理批量编码、写缓冲复用、队列上限、慢消费者、失败重试、跨节点房间路由和消息有序性。

## 规模估算

假设热门直播间存在 `300 万`在线连接、每秒产生 `10 万`条弹幕，不能简单按 `10 万 × 300 万`逐条同步广播。真实系统通常会采用：

- 房间分片与多级扇出。
- 网关本地广播，避免每条消息重复跨网络传输。
- 批量写入与共享编码结果。
- 热门房间拆分、慢连接治理和背压。
- Kafka 等消息系统进行服务解耦，而不是直接承担每个客户端的最终投递。

---

<br/>

> <h3 id="技术选型">技术选型</h3>

## WebSocket 库选择

- **中小项目、快速开发：**优先使用 `gorilla/websocket` 或 `coder/websocket` 等高层库。
- **需要自定义事件循环、缓冲区和协议处理：**考虑 `gobwas/ws`。
- **大规模连接网关：**可采用 `gnet + WebSocket Codec`，但必须正确处理增量握手、Frame 解析、背压和连接状态，不能把阻塞式 API 直接塞进事件循环。

`gobwas/ws` 不是必选项。它的价值在于提供更底层的控制能力，同时也意味着更高的实现和维护成本。

## `gnet` 与 `netpoll`

二者解决的问题不同，不应简单二选一：

- **`gnet`：**适合实现事件驱动的 TCP/WebSocket 长连接网关。
- **`netpoll`：**常作为高性能 RPC/网络库的底层能力，例如与 Kitex 组合用于内部服务通信。

网关链路：

```text
用户
 |
WebSocket
 |
Gateway
```

内部 RPC 链路：

```text
Video Service
      |
User Service
```

```text
Kitex
  |
netpoll
  |
 RPC
```

## 推荐技术栈

| 层级       | 技术方向                                            |
| ---------- | --------------------------------------------------- |
| Gateway    | `gnet/v2 + gobwas/ws` 或成熟的 gnet WebSocket Codec |
| RPC        | `Kitex + netpoll`                                   |
| MQ         | Kafka                                               |
| Cache      | Redis Cluster                                       |
| 分析存储   | ClickHouse                                          |
| 业务数据库 | MySQL，用于用户和视频元数据                         |

最终架构：

```text
gnet
  |
TCP 事件循环
  ↓
gobwas/ws
  |
WebSocket 协议
  ↓
Room Manager
  ↓
Kafka
  ↓
Redis + ClickHouse
```

**核心结论：**`gnet` 负责连接与事件循环，`gobwas/ws` 负责 WebSocket 协议，房间和消息系统负责业务广播；`netpoll` 更适合内部 RPC 网络层。大型实时系统可以同时使用这些组件，但必须按职责分层，而不是将 API 直接硬拼。

---

<br/><br/><br/>

> <h2 id="弹幕V2修改点">弹幕V2修改点</h2>
> ‌[BóYí](๑•̀ㅂ•́)و✧

已结合 `/Users/ganghuang/Desktop/Temp.md` 完成第一阶段可落地优化。当前改动重点处理单机网关的锁竞争、广播内存放大、慢客户端隔离、请求突刺和亿级 MySQL 热表点查。

未声称当前实现已经实测支撑千万连接。千万并发仍需要多实例网关、房间亲和路由、Kafka 分区、Redis Cluster、内核与网络调优以及分阶段压测共同完成。

**1. 修改了哪些文件**

- `config/base/app.yaml`
- `internal/pkg/config/hg_env_config.go`
- `internal/pkg/config/hg_video_danmaku_config_test.go`
- `internal/modules/video_danmaku/realtime/hg_gnet_server.go`
- `internal/modules/video_danmaku/realtime/hg_gnet_server_test.go`
- `internal/modules/video_danmaku/repository/hg_video_danmaku_repository.go`
- `internal/modules/video_danmaku/repository/hg_video_danmaku_repository_test.go`
- `internal/pkg/mysql/queries/hg_video_danmaku_queries.go`

前端仓库未修改。本轮保持现有 WebSocket 消息格式和前端发送行为兼容。

**2. 做了什么改动**

- 将全局房间锁拆成默认 `256` 个房间目录分片。
- 每个目录分片设置默认 `64` 个共享成员分片。
- 移除每次广播创建全房间 `[]target` 快照的逻辑。
- 广播只锁定对应视频的一个目录分片，并逐成员分片发送。
- 成员分片资源固定，不再为每个长尾视频预分配 64 个 map 和锁。
- 保留每连接出站积压统计，并将硬编码的 `64 KiB` 改为可配置。
- 慢客户端超过出站预算后主动关闭，防止热门房间广播导致 OOM。
- 慢客户端关闭延后到成员锁释放后执行，避免潜在锁重入。
- 新增 event-loop 内无锁令牌桶，在进入共享 worker 队列和 MySQL 前拒绝单连接突刺。
- 默认限流为每连接每秒 `5` 条、突发 `10` 条。
- ACK、error 和房间广播改为具体结构体序列化，移除 `map[string]any`。
- 房间路由使用内联 FNV-1a，避免每次查找创建哈希对象。
- 单连接限流时间改为 `int64` 纳秒时间戳，降低连接常驻状态体积。
- 新弹幕插入后使用 `LastInsertId()` 按聚簇主键回读。
- 不再通过 `danmaku_id` 二级唯一索引回读刚插入的亿级热表记录。
- 增加配置边界、房间分布、限流和主键回读测试。
- 增加 ACK 序列化分配基准。

新增配置：

```yaml
room_shard_count: 256
member_shard_count: 64
max_pending_bytes: 65536
command_rate_per_second: 5
command_burst: 10
```

房间和成员分片数必须是二次幂，并有明确上下限，避免错误配置制造过多锁、map 或内存常驻对象。

**3. 为什么这样改**

原实现的主要风险是：

- 所有视频共享一把 `roomsMu`。
- 热门房间每次广播都会遍历房间并复制完整目标列表。
- 百万连接房间会产生与在线人数线性增长的短命切片。
- 热门房间广播会阻塞其他视频的加入和离开。
- 慢客户端的积压预算不可配置。
- 单个恶意或异常客户端可以持续占用共享命令队列和数据库连接。
- 新记录插入后还需要经过宽字符串二级索引回读。

修改后：

- 不同视频大概率落入不同目录锁，跨视频竞争显著降低。
- 广播不再创建 O(N) 目标快照，降低大房间的 GC 和内存带宽压力。
- 长尾视频不会因每房间预分配大量分片对象产生反向内存放大。
- 限流在 gnet event-loop 内完成，不访问 Redis、MySQL，也不创建额外 goroutine。
- 过载压力不会无条件传导到 worker 队列和 MySQL。
- `LastInsertId()` 回读命中 InnoDB 聚簇主键，更适合亿级热表。

**4. 准确性检查结果**

- 保留现有 `danmaku.create`、`danmaku.ack`、`danmaku.created` 和 `error` JSON 字段。
- 保留 `requestId` 幂等与请求关联语义。
- 保留 MySQL 提交成功后才广播的语义。
- 保留 Redis 跨实例广播和 MySQL 历史恢复机制。
- 保留视频公开状态和 `close_danmaku` 校验。
- 保留 `(user_id, request_id)` 唯一键幂等。
- 保留 64 个统计分片及原事务边界。
- 房间空闲后仍会从目录中删除。
- 房间删除、立即重建和并发广播由同一个目录分片锁保护。
- 慢连接不会在成员锁持有期间直接关闭。
- `git diff --check`：通过。
- 当前工作区仅包含本轮 8 个目标文件的修改，没有提交，也没有推送。

**5. 潜在影响**

- 单连接超过默认发送频率时将收到：

```json
{
  "type": "error",
  "requestId": "...",
  "data": {
    "code": "rate_limited",
    "message": "弹幕发送过于频繁，请稍后重试"
  }
}
```

- 这是明确的 WebSocket 业务拒绝，现有前端不会继续 HTTP 回退。
- 配置缺少新增字段时，应用会因资源边界校验失败而拒绝启动。项目基础配置已补齐；独立覆盖整个 `video_danmaku` 配置块的外部环境需要同步增加这些字段。
- 广播仍然必须执行 O(N) 连接写入，这是向 N 个客户端推送数据本身不可消除的成本。
- 当前每连接仍调用一次 `AsyncWrite`。下一阶段需要按 gnet event-loop 建立有界批量出站队列，进一步减少跨 event-loop 调度。
- Redis 仍使用单个全局 Pub/Sub 频道，网关实例数增加后会产生全量消息放大。
- MySQL 统计分片更新仍在创建事务内，热门视频极端写入时仍可能出现行锁竞争。
- 没有执行真实百万或千万连接压测，不能将代码设计优化等同于容量验证。

**6. 格式化/编译/测试说明**

已执行：

```text
gofmt
go test ./internal/modules/video_danmaku/... ./internal/pkg/config ./internal/pkg/mysql/queries
go vet ./internal/modules/video_danmaku/... ./internal/pkg/config ./internal/pkg/mysql/queries
go test -race ./internal/modules/video_danmaku/realtime ./internal/modules/video_danmaku/repository
go test -run '^$' -bench 'BenchmarkCommandAckPayload' -benchmem ./internal/modules/video_danmaku/realtime
git diff --check
```

结果：

- 受影响 Go 测试：通过。
- `go vet`：通过。
- realtime 和 repository race test：通过。
- 配置边界测试：通过。
- 房间目录分布和限流测试连续执行 20 次：通过。
- SQL 主键回读测试：通过。
- 差异空白检查：通过。

当前 ACK 序列化基准，Apple M2 Pro：

```text
BenchmarkCommandAckPayload-10
3259953 iterations
363.5 ns/op
400 B/op
2 allocs/op
```

该基准只覆盖 ACK JSON 生成，不代表端到端网络广播吞吐。

**7. 后续生产级优化建议**

1. 将 Redis 单一全局频道改为本地活跃房间动态订阅，避免每台网关接收全平台所有弹幕。
2. 按 gnet event-loop 建立有界广播队列，一次唤醒批量写多个连接，减少每连接一次 `AsyncWrite` 调度。
3. 增加命令队列深度、拒绝数、慢客户端关闭数、广播接收人数、锁等待和房间数量指标。
4. 修复现有 Outbox 每秒实际约 8 条的吞吐限制，再接入 `video.danmaku.created.v1` Kafka 事件。
5. 将计数更新移出弹幕创建事务，由 Kafka consumer 批量维护 Redis/MySQL 投影。
6. 为视频可弹幕状态增加短 TTL 本地加 Redis 缓存，降低创建和列表接口的重复 JOIN。
7. 引入按 `video_id` 的 Kafka partition 和网关 room affinity，避免广播全网扩散。
8. 亿级以上历史数据采用 MySQL 热数据分片、ClickHouse 冷历史和有界归档，不依赖单张 InnoDB 表无限增长。
9. 使用 10 万、50 万、100 万连接分阶段压测，采集单连接内存、广播 P99、GC、mutex/block profile、Redis 带宽和 MySQL 锁等待，再决定单机安全连接上限。



<br/>

---

<br/><br/><br/>
> # <h1 id="B站级弹幕系统设计V3">B 站级弹幕系统设计 V3</h1>


***
<br/><br/><br/>

> <h2 id="Go实时消息平台架构设计">Go 实时消息平台架构设计</h2>

面向 B 站弹幕、直播聊天室、游戏实时消息、IM 群聊和点赞通知等场景，如果目标包括**千万级 WebSocket 长连接、亿级弹幕数据、毫秒级广播和持续扩展能力**，就不应只设计一个 WebSocket Server，而应建设完整的**实时消息平台（Realtime Messaging Platform）**。

**核心原则：** Gateway 只维护连接和转发消息；Room Worker 负责本地实时广播；Kafka 承担异步解耦和持久化链路；Redis 保存热点状态；ClickHouse 存储海量历史消息。

***
<br/>

> <h3 id="整体架构">整体架构</h3>

```text
                         Client
                            |
                            |
                    WebSocket Connection
                            |
                            |
                +------------------------+
                |   Danmaku Gateway      |
                |   (gnet + gobwas/ws)   |
                +------------------------+
                            |
                            |
                    Room Router
                            |
             +--------------+--------------+
             |                             |
             |                             |
       Room Worker Pool              Connection Manager
             |
             |
          Kafka Producer
             |
             |
        Kafka Topic
             |
             |
 +-----------------------------+
 |                             |
 |                             |
Danmaku Consumer          Analytics Consumer
 |
 |
 +-------------+
 |
 |
Redis Cluster
 |
 |
ClickHouse
 |
 |
MySQL
```

这套架构将实时广播与异步数据处理分离：消息进入 Gateway 后由 Room Worker 直接广播，同时投递 Kafka，供存储、分析等消费者异步处理。

***
<br/>

> <h3 id="工程与服务拆分">工程与服务拆分</h3>

生产环境不建议使用单体架构，可按业务职责拆分：

```text
bilibili-realtime/
├── gateway
├── room-service
├── danmaku-service
├── user-service
├── online-service
├── analytics-service
├── common
└── deploy
```

- **Gateway：** 基于 `gnet`、`gobwas/ws` 维护海量长连接。
- **Room Service：** 基于 `goroutine + channel` 管理房间状态、消息排序和广播。
- **Danmaku Service：** 消费 Kafka 消息并写入 ClickHouse 等存储。
- **User Service：** 提供用户、鉴权及相关业务能力。
- **Online Service：** 统计在线连接数或近似去重用户数。
- **Analytics Service：** 异步消费消息并完成指标分析。

***
<br/>

> <h3 id="Gateway与连接模型">Gateway 与连接模型</h3>

### Gateway 职责

Gateway 只负责：

1. TCP 连接。
2. WebSocket 协议处理。
3. 心跳检测。
4. 登录鉴权。
5. 加入或退出房间。
6. 消息转发。

Gateway 不应直接写数据库、操作 Kafka 或查询完整用户资料，避免连接层被业务逻辑阻塞。可参考以下目录：

```text
gateway/
├── main.go
├── server.go
├── connection.go
├── websocket.go
├── heartbeat.go
├── router.go
└── middleware/
```

---
<br/>

### Connection 模型

千万级连接不适合集中存放在一个全局 `map[userID]*Conn` 中，应按 Gateway 实例、房间或分片组织连接，降低锁竞争和单点内存压力。

```go
type Connection struct {
    ID            int64
    UserID        int64
    RoomID        int64
    Conn          gnet.Conn
    SendQueue     chan []byte
    LastHeartbeat int64
}
```

连接管理器可维护房间分片：

```go
type ConnectionManager struct {
    rooms sync.Map
}
```

```text
RoomManager

room:10001
       |
       |
       +-- conn
       |
       +-- conn
       |
       +-- conn
```

实际生产中还需限制 `SendQueue` 容量，并通过连接分片、事件循环归属或单线程房间 Worker 控制并发访问。

***
<br/>

> <h3 id="RoomManager与广播模型">Room Manager 与广播模型</h3>

一个视频或直播间对应一个 `room_id`。例如视频 `BV1xxxx` 可映射到 `room:888888`：

```go
type Room struct {
    ID           int64
    Clients      sync.Map
    MessageQueue chan *Message
}
```

热门房间可分配独立 Worker，由单独的 goroutine 处理广播、限流和排序：

```text
热门直播

room worker

goroutine

负责:
- 广播
- 限流
- 排序
```

---
<br/>

### 广播模型

不要在广播主路径中同步向所有客户端写数据：

```go
for client := range clients {
    client.Write(msg)
}
```

任意慢客户端都可能阻塞整个房间。正确方式是为每个连接维护独立的有界发送队列，广播过程只执行非阻塞投递：

```go
type Connection struct {
    SendQueue chan []byte
}
```

```go
func Broadcast(msg []byte) {
    room.Clients.Range(func(_, v any) bool {
        conn := v.(*Connection)

        select {
        case conn.SendQueue <- msg:
        default:
            // 丢弃慢客户端
        }

        return true
    })
}
```

队列满时可按业务要求丢弃消息、合并消息或断开慢客户端，避免单个连接拖垮整个房间。百万级热门房间还需进一步采用房间分片、广播树或多级 Fan-out，单机遍历所有连接并不足以支撑跨节点广播。

***
<br/>

> <h3 id="消息协议与发送流程">消息协议与发送流程</h3>

### Protobuf 二进制协议

大规模实时消息不建议使用 JSON 作为核心传输格式，因为字段名和文本解析会增加带宽及 CPU 消耗。可使用 Protobuf：

```protobuf
message Danmaku {
  int64 user_id = 1;
  int64 room_id = 2;
  string content = 3;
  int64 timestamp = 4;
}
```

```text
protobuf
      |
      |
Go struct
```

客户端通过 WebSocket Binary Frame 发送 Protobuf 字节：

```text
Binary Frame

protobuf bytes
```

而不是发送以下 JSON：

```json
{
  "text": "666"
}
```

实际协议还应包含消息类型、协议版本、请求 ID、压缩标记和载荷长度等信封字段。

---
<br/>

### 发送链路

用户发送弹幕后的异步数据链路：

```text
APP
 |
 |
WebSocket
 |
 |
Gateway
 |
 |
Room Worker
 |
 |
Kafka
 |
 |
Consumer
 |
 |
Redis
```

**广播不要经过 Kafka。** Kafka 会增加实时广播延迟，应由 Room Worker 在本地或实时 Fan-out 网络中直接广播；Kafka 负责异步存储、分析和事件分发。

```text
              send

              |
              |

Gateway

              |

Room Worker

              |

100万人连接
```

典型处理顺序：

1. Gateway 完成鉴权、协议解析和基础校验。
2. Room Worker 执行限流、排序并实时广播。
3. 消息异步投递 Kafka。
4. 消费者分别更新热点数据、持久化历史消息和生成分析指标。

***
<br/>

> <h3 id="KafkaRedis与ClickHouse">Kafka、Redis 与 ClickHouse</h3>

### Kafka：异步消息总线

Topic：

```text
danmaku_message
```

按照 `room_id` 计算 Partition：

```text
partition = hash(roomID) % N
```

同一个房间的消息进入同一 Partition，可在 Kafka Partition 范围内保持顺序。

```go
type KafkaMessage struct {
    RoomID   int64
    UserID   int64
    Content  string
    Timestamp int64
}
```

---
<br/>

### Redis：热点状态

Redis 不保存所有历史弹幕，只保存热点数据。

**在线人数：**

```text
Key:   room:online:{roomID}
Value: 500000
```

连接建立和断开时可使用计数器维护在线连接数。`HyperLogLog` 适合统计近似去重用户数，但不适合要求精确增减的实时在线人数。

**最近弹幕：**

使用 Redis Stream 保存房间最近的 `1000` 条弹幕：

```text
room:danmaku:{roomID}
```

```sh
XADD room:danmaku:888 *
```

写入后可结合 `MAXLEN` 或定期裁剪控制 Stream 长度。

---
<br/>

### ClickHouse：历史消息

亿级弹幕历史数据不宜全部写入 MySQL，可使用 ClickHouse：

```sql
CREATE TABLE danmaku_history
(
    id UInt64,
    room_id UInt64,
    user_id UInt64,
    content String,
    create_time DateTime
)
ENGINE = MergeTree
PARTITION BY toYYYYMM(create_time)
ORDER BY
(
    room_id,
    create_time
);
```

按房间和时间查询视频回放弹幕：

```sql
SELECT *
FROM danmaku_history
WHERE room_id = 888
ORDER BY create_time;
```

生产环境通常由 Kafka Consumer 聚合批量写入 ClickHouse，避免逐条插入。

***
<br/>

> <h3 id="限流与可观测性">限流与可观测性</h3>

### 多级限流

为防止刷屏，可使用本地令牌桶与 Redis 分布式限流组合控制：

```text
danmaku:rate:{userID}
```

- **用户级：** 例如每个用户 `5 条/秒`。
- **房间级：** 例如每个房间 `10 万条/秒`。
- **连接级：** 限制单连接消息频率、帧大小和发送队列长度。
- **系统级：** 过载时执行降级、采样、丢弃低优先级消息或拒绝新连接。

### 可观测性

至少监控以下指标：

- 当前连接数、连接建立和断开速率。
- 各房间消息吞吐量与广播延迟。
- 发送队列长度、队列满次数和慢客户端数量。
- Kafka Producer 延迟、积压量和消费失败率。
- Redis、ClickHouse 的写入延迟与错误率。
- Goroutine 数量、GC、内存和网络流量。

使用 Prometheus 采集指标，OpenTelemetry 记录跨服务链路，并支持优雅关闭和连接迁移。

***
<br/>

> <h3 id="AICodingAgent提示词">AI Coding Agent 提示词</h3>

可将以下需求交给 Claude Code、Codex、Cursor 等 AI Coding Agent 生成项目骨架：

```text
请生成一个 Go 微服务项目：

名称：
bilibili-realtime

技术：
- Go 1.24
- gnet/v2
- gobwas/ws
- Kafka franz-go
- Redis go-redis/v9
- ClickHouse driver
- protobuf

实现：
1. WebSocket Gateway
2. Room Manager
3. Connection Manager
4. Kafka Producer
5. Kafka Consumer
6. Redis Online Counter
7. ClickHouse Storage

要求：
- 按 100 万连接目标设计
- 使用事件驱动模型
- 尽量减少共享锁竞争
- 对高频临时对象合理使用 sync.Pool
- 使用 protobuf binary protocol
- 支持优雅关闭
- 接入 Prometheus metrics
```

“无锁设计”和 `sync.Pool` 不应作为绝对目标，应先保证连接归属、并发模型和内存生命周期正确，再通过压测决定优化方案。

***
<br/>

> <h3 id="生产级技术选型">生产级技术选型</h3>

| 模块 | 技术 |
| --- | --- |
| TCP 事件循环 | gnet/v2 |
| WebSocket | gobwas/ws |
| 协议 | Protobuf |
| RPC | gRPC / Kitex |
| MQ | Kafka |
| 热点状态 | Redis Cluster |
| 历史存储 | ClickHouse |
| 监控 | Prometheus |
| 链路追踪 | OpenTelemetry |
| 部署 | Kubernetes |

容量不能只按“连接数”评估，还需同时计算每台 Gateway 的文件描述符、内存、心跳流量、出站带宽、热门房间扇出量和跨节点广播成本。

***
<br/>

> <h3 id="最终架构">最终架构</h3>

```text
                 App

                  |

            WebSocket

                  |

        +----------------+
        | gnet Gateway   |
        +----------------+

                  |

          Room Manager

                  |

          Local Broadcast

                  |

        Millions Connections



                  |

             Kafka Async

                  |

        Danmaku Storage
          /             \

     Redis             ClickHouse
```

该架构的职责边界如下：

- **Gateway：** 长连接、协议、心跳、鉴权和转发。
- **Room Worker：** 实时广播、排序、限流和慢客户端隔离。
- **Kafka：** 异步解耦存储、分析和扩展业务。
- **Redis：** 在线状态、最近消息等热点数据。
- **ClickHouse：** 海量历史消息和回放查询。

**核心结论：实时链路追求低延迟，持久化链路追求可靠解耦，两者必须分离。**



---
<br/><br/><br/>
> <h2 id="弹幕V3修改点">弹幕V3修改点</h2>

‌BóYí (๑•̀ㅂ•́)و✧
1. 修改了哪些文件
核心网关与实时路由：
- internal/modules/video_danmaku/realtime/hg_gnet_server.go
- internal/modules/video_danmaku/realtime/hg_room_router.go
- internal/modules/video_danmaku/realtime/hg_gnet_server_test.go
- internal/modules/video_danmaku/module/hg_video_danmaku_module.go
Kafka、Outbox 与消费链路：
- internal/events/video_danmaku/hg_video_danmaku_event.go
- internal/consumer/danmaku/hg_danmaku_consumer.go
- internal/consumer/danmaku/hg_recent_projector.go
- internal/consumer/danmaku/hg_danmaku_consumer_test.go
- internal/infrastructure/kafka/hg_runtime.go
- internal/pkg/kafka/hg_config.go
- internal/pkg/config/hg_kafka_config.go
- internal/outbox/hg_outbox_dispatcher.go
- hg_kafka_application.go
数据存储与 Redis：
- internal/modules/video_danmaku/repository/hg_video_danmaku_repository.go
- internal/pkg/clickhouse/hg_client.go
- internal/pkg/clickhouse/hg_client_test.go
- internal/pkg/redis/hg_redis_key.go
- internal/pkg/redis/hg_redis_script.go
- internal/pkg/redis/hg_video_danmaku_key_test.go
- deployments/clickhouse/002_video_danmaku_history.sql
配置、脚本与文档：
- config/base/app.yaml
- config/base/clickhouse.yaml
- config/debug/kafka.yaml
- config/pre/kafka.yaml
- config/prod/kafka.yaml
- internal/pkg/config/hg_env_config.go
- scripts/kafka_init.sh
- scripts/kafka_init_test.sh
- HG_Docuemnts/视频弹幕千万并发与亿级存储部署.md
工作区原有的 HG_Docuemnts/视频弹幕版本.md 修改未由本轮产生，未覆盖或回退。
2. 做了什么改动
- 将 Redis 全局广播频道改为按视频动态频道：video:danmaku:broadcast:{videoID}:v2。
- 本实例首个房间连接建立时订阅，最后一个连接离开时异步退订。
- Redis 订阅线程不再直接遍历热门房间，而是投递到固定 64 个广播分片 worker。
- 同一视频稳定进入同一个广播 worker，保持单实例内广播顺序。
- 广播队列有容量上限，过载时丢弃可恢复的实时副本，不阻塞 Redis 订阅和 gnet event-loop。
- 保留现有房间目录、成员分片、慢客户端字节预算及 JSON WebSocket 协议。
- MySQL 弹幕记录和 Outbox 事件现在可以在同一事务中提交。
- 新增稳定事件 video.danmaku.created，Kafka partition key 为 video_id。
- 新增专用 Kafka Topic mlc.video.danmaku.created.v1，默认 96 partitions、3 replicas。
- Kafka Consumer 按同一 partition 的有界批次写入 ClickHouse。
- 新增 ClickHouse 表 video_danmaku_history，按月分区，排序键为：
(video_id, progress_ms, created_at, danmaku_id)。
- ClickHouse 使用 ReplacingMergeTree(ingested_at)，支持 Kafka 至少一次投递和历史重放。
- 新增 ClickHouse 历史查询能力，限制为最大 5 分钟窗口、1000 条、复合游标分页，禁止深度 OFFSET。
- Kafka 消费成功后，将最近弹幕投影到 Redis Stream。
- Redis Stream 默认仅保留最近约 1000 条，避免将完整历史堆入 Redis。
- Redis Lua 使用 topic:partition -> offset 水位实现永久幂等投影。
- Stream key 和 offset key 使用相同 {videoID} hash tag，兼容 Redis Cluster slot 约束。
- Outbox dispatcher 从 8 worker 提升到固定 64 worker。
- Outbox 有积压时持续排空，只有领取不到事件时才等待轮询间隔。
- 新增完整部署、灰度、回滚、房间亲和、容量计算和压测说明。
3. 为什么这样改
原有实现存在两个主要扩展瓶颈：
- 每个 Gateway 都订阅全平台广播频道，实例越多，Redis 流量和反序列化工作量越大。
- MySQL 单表不能作为亿万至百亿级弹幕历史的长期唯一存储。
新链路形成以下职责边界：
实时广播：
Client -> gnet Gateway -> local room broadcast
                       -> Redis room channel -> other subscribed gateways

可靠历史：
MySQL transaction -> Outbox -> Kafka
                           -> ClickHouse complete history
                           -> Redis recent Stream
这样可以通过增加 Gateway Pod 横向扩展连接数，通过 Kafka partition 和 Consumer 扩展历史写入吞吐，并将大规模时间线查询转移到 ClickHouse。
4. 准确性检查结果
已确认：
- 同一视频的 Redis 广播、Stream 和 offset key 具有一致的 Cluster hash tag。
- 首个连接只有在 Redis 房间订阅成功后才完成 WebSocket 加入。
- 并发第二个连接不会提前绕过首个订阅确认。
- gnet event-loop 不执行退订 Redis I/O。
- Redis 收包不会同步阻塞在百万连接房间广播上。
- MySQL 数据与 Outbox 事件处于同一事务。
- 幂等重放不会重复生成 Outbox 事件。
- Kafka Consumer 先写 ClickHouse，再推进 Redis offset 投影，失败时不提交 Kafka offset。
- ClickHouse 使用批量 JSONEachRow，不是逐条 HTTP 请求。
- ClickHouse 查询使用稳定复合游标，不使用深分页。
- 现有 HTTP、JSON WebSocket、ACK 和 MySQL 幂等语义未改变。
- git diff --check 通过。
- 未发现密钥、密码或临时编译产物写入仓库。
5. 潜在影响
- 弹幕历史 Kafka Consumer 在 debug、pre、prod 中默认保持 enabled: false。
- 启用消费者前必须先执行 deployments/clickhouse/002_video_danmaku_history.sql。
- 当前 HTTP 时间窗查询仍读取 MySQL 热数据，ClickHouse 查询能力已经实现，但尚未强制切换外部查询路径。
- Redis Pub/Sub 仍是实时非可靠传输；断线恢复依赖 MySQL、Redis recent 或 ClickHouse。
- 当前仍保持同步 MySQL 权威创建语义，以兼容现有 ACK。Kafka 和 ClickHouse承担异步历史归档，而不是将旧客户端 ACK 改成“Kafka 已接收”。
- 96 partitions 是默认部署值，最终数量必须根据 Kafka broker 数、写入量和 Consumer 并发确定。
- 单个超级热门视频仍会集中到一个 Kafka partition。达到单 partition 上限后需要 lane 分片，并明确是否接受跨 lane 弱顺序。
- ClickHouse 默认保留 1825 天，生产需根据磁盘、合规和备份策略调整。
- 本轮没有执行真实 Kafka、Redis Cluster、ClickHouse 集成环境写入，也没有执行百万连接压测。
- 当前结果应描述为“已实现支持横向扩展的千万连接和亿级历史架构路径”，不能描述为“当前单机已经实测支持千万连接”。
6. 格式化、编译、测试说明
已执行并通过：
gofmt
go test ./internal/modules/video_danmaku/...
go test ./internal/consumer/danmaku
go test ./internal/infrastructure/kafka
go test ./internal/outbox
go test ./internal/pkg/config
go test ./internal/pkg/clickhouse
go test ./internal/pkg/kafka
go test ./internal/pkg/redis
Race 检查通过：
go test -race ./internal/modules/video_danmaku/realtime
go test -race ./internal/consumer/danmaku
go test -race ./internal/outbox
静态检查通过：
go vet ./internal/modules/video_danmaku/... \
  ./internal/consumer/danmaku \
  ./internal/infrastructure/kafka \
  ./internal/outbox \
  ./internal/pkg/config \
  ./internal/pkg/clickhouse \
  ./internal/pkg/kafka \
  ./internal/pkg/redis
脚本验证通过：
bash -n scripts/kafka_init.sh scripts/kafka_init_test.sh
bash scripts/kafka_init_test.sh
结果：
[PASS] Kafka Topic 拓扑校验通过
本轮未提交、未 push。
7. 后续生产级优化建议
1. 按文档先创建 Kafka Topic 和 ClickHouse 表，再从单副本灰度启用弹幕历史 Consumer。
2. 为 WebSocket Ingress 增加基于 videoId 的 consistent hash，降低同房间跨 Pod 广播比例。
3. 增加弹幕专属 Prometheus 指标和告警，包括广播队列丢弃、Outbox pending/dead、Kafka lag、ClickHouse 写延迟和 Redis 投影失败。
4. 完成 MySQL 与 ClickHouse 双写对账及历史回灌后，将旧时间窗查询路由到 ClickHouse。
5. 使用 10 万、50 万、100 万连接分阶段压测，采集单连接内存、广播 P99、GC、网络带宽、Kafka partition 热点和 ClickHouse merge 压力，再确定每 Pod 安全连接数及千万连接所需副本数。