# 数据层规则

## 数据层
- 数据访问层写法保持一致
- 不擅改 SQL、ORM、查询封装方式
- 不擅改 repository / dao 职责边界

## SQL / ORM
- 查询风格保持一致
- 不擅改字段映射、筛选、排序、分页语义
- 不因局部优化改变查询含义
- 涉及事务优先沿用现有模式

## 兼容
- 不擅改数据库字段含义
- 不擅改 model 与库字段映射
- 不擅改序列化字段和 tag

## 模型
- 不混用数据库模型、接口模型、业务模型
- 不随意合并或拆分模型