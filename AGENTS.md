# AGENTS.md

## 适用范围
本文件是当前项目中 AI 编码助手（如 Codex）工作的主入口规则。
在本项目内进行代码阅读、分析、修改、补全、重构、排障、生成代码时，必须优先遵循本文件及 `.ai_agents/` 目录下的模块化规则文件。

---

## 规则组织方式
本项目采用“主入口 + 模块化规则文件”的方式组织开发约束：

- `AGENTS.md`：总入口，定义执行原则、优先级和使用方式
- `.ai_agents/00-core.md`：核心工作原则
- `.ai_agents/01-style.md`：代码风格、命名、注释、日志规则
- `.ai_agents/02-go-rules.md`：Go 语言专项规则
- `.ai_agents/03-api-rules.md`：API / Handler / Service 分层规则
- `.ai_agents/04-data-rules.md`：Repository / DAO / DB 相关规则
- `.ai_agents/05-validation.md`：格式化、编译、测试、自检规则
- `.ai_agents/06-output.md`：任务完成后的输出说明规范
- `.ai_agents/07-forbidden.md`：明确禁止事项

---

## 执行原则
在本项目中工作时，必须遵循以下原则：

1. 先阅读 `AGENTS.md`
2. 根据任务类型，继续遵循相关模块文件
3. 若多个模块都适用，则共同生效
4. 若规则冲突，按优先级处理
5. 未明确允许的高风险改动，默认不执行

---

## 规则优先级
规则优先级从高到低如下：

1. 用户当前明确任务要求
2. `AGENTS.md`
3. `.ai_agents/00-core.md`
4. 其他 `.ai_agents/*.md` 模块规则
5. 项目内已有代码风格与相似实现

若规则之间出现冲突，应优先选择：

- 更保守的做法
- 更兼容现有项目的做法
- 更小改动的做法

---

## 按任务类型加载规则
执行任务时，至少按以下方式理解和应用规则：

### 所有任务都必须遵循
- `AGENTS.md`
- `.ai_agents/00-core.md`
- `.ai_agents/01-style.md`
- `.ai_agents/05-validation.md`
- `.ai_agents/06-output.md`
- `.ai_agents/07-forbidden.md`

### 涉及 Go 代码时，额外必须遵循
- `.ai_agents/02-go-rules.md`

### 涉及 HTTP API、Handler、Controller、Service 时，额外必须遵循
- `.ai_agents/03-api-rules.md`

### 涉及 Repository、DAO、数据库、SQL、ORM、数据映射时，额外必须遵循
- `.ai_agents/04-data-rules.md`

### 涉及 Shell、Bash、启动脚本、运维脚本时，额外必须关注
- 脚本可读性与注释完整性
- 优先补充关键流程、命令目的、分支逻辑、失败处理说明
- 默认假设维护者可能不熟悉脚本语法，因此新增或修改脚本时应提供足够注释帮助理解

---

## 标准执行流程
在本项目中处理任何开发任务时，默认执行流程如下：

1. 阅读当前任务描述
2. 阅读 `AGENTS.md`
3. 根据任务类型匹配需要遵循的模块文件
4. 阅读相关代码上下文、调用链、相似实现
5. 按“最小必要改动”原则实施修改
6. 修改后执行格式化、自检、尽可能的编译/测试验证
7. 按 `.ai_agents/06-output.md` 输出真实结果说明

---

## 默认行为要求
除非任务明确要求，否则默认必须满足以下要求：

- 本项目按高并发后端工程处理；若发现不适合百万级请求的架构、实现或方法，先用最少文字给出问题、建议、收益/风险，待开发者同意后再改
- 改动优先采用主流稳定的后端工程架构、常用优秀设计模式和业内通用实践，避免生硬套用前端思路
- 经确认后，必须在当前基础上完成代码落地，并进行必要的测试、验证与结果说明
- 使用当前项目代码风格，最好是GO语言api风格
- 方法、变量、API 命名延续Go语言系统API风格
- 新增或修改方法、函数时，必须补充通俗易懂的简洁注释，说明职责、核心校验点或设计目的
- 不过度优化设计架构
- 修改后必须仔细检查代码
- 修改后必须格式化代码
- 新增或修改脚本时，必须补充足够注释，保证未系统学习脚本的维护者也能读懂关键逻辑
- 调用系统方法、变量和新增的方法、变量、必须补充通俗易懂的简洁注释说明职责与目的（遵循 `.ai_agents/01-style.md`）
- 有条件时必须尽可能验证编译和测试
- 未执行的验证不得声称已通过

---

## 模块化执行要求
执行任务时，不得只参考单一规则文件而忽略相关模块。

具体要求如下：

- 代码修改类任务：必须同时遵循核心规则、风格规则、语言规则、验证规则、输出规则、禁止规则
- API 改动类任务：必须额外遵循 API 分层规则
- 数据访问类任务：必须额外遵循数据层规则
- 若一次任务同时涉及 API 与数据层，则相关模块规则同时生效
- 若无法确定适用模块，默认采用更严格、更保守的规则

---

## 风险控制要求
以下行为默认视为高风险，除非任务明确要求，否则不应执行：

- 大规模重构
- 修改目录结构
- 修改模块边界
- 修改公共接口
- 修改对外字段名或 tag
- 引入第三方依赖
- 改写并发模型
- 修改事务、幂等、重试、回滚逻辑
- 修改响应结构或错误语义

## 临时文件与缓存要求
- 默认不得将临时缓存、编译缓存、测试缓存、调试产物写入工程目录
- 若必须临时生成缓存文件，应优先使用系统临时目录或工程外缓存目录
- 不得把 `.codex_gocache`、`.gocache`、临时日志、临时二进制等产物留在项目内

---

## 最终要求
完成任务后，必须：

- 按模块规则完成自检
- 进行格式化
- 尽可能执行编译/测试验证
- 真实说明改动内容和验证情况
- 输出说明必须遵循 `.ai_agents/06-output.md` 的固定 1~6 模板

不得把“未执行的检查”表述为“已通过”。
