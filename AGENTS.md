# AGENTS.md

## 适用范围
本文件是当前项目中 AI 编码助手（如 Codex）工作的主入口规则。
在本项目内进行代码阅读、分析、修改、补全、重构、排障、生成代码时，必须优先遵循本文件及 `.ai_agents/` 目录下的模块化规则文件。

---

## 规则组织方式
本项目采用“主入口 + 模块化规则文件”的方式组织开发约束：

- `AGENTS.md`：总入口，定义执行原则、优先级和使用方式
- `.ai_agents/00-core.md`：核心工作原则
- `.ai_agents/01-style.md`：代码风格、命名、注释、日志规则
- `.ai_agents/02-go-rules.md`：Go 语言专项规则
- `.ai_agents/03-api-rules.md`：API / Handler / Service 分层规则
- `.ai_agents/04-data-rules.md`：Repository / DAO / DB 相关规则
- `.ai_agents/05-validation.md`：格式化、编译、测试、自检规则
- `.ai_agents/06-output.md`：任务完成后的输出说明规范
- `.ai_agents/07-forbidden.md`：明确禁止事项

---

## 执行原则
在本项目中工作时，必须遵循以下原则：

1. 先阅读 `AGENTS.md`
2. 根据任务类型，继续遵循相关模块文件
3. 若多个模块都适用，则共同生效
4. 若规则冲突，按优先级处理
5. 未明确允许的高风险改动，默认不执行

---

## 规则优先级
规则优先级从高到低如下：

1. 用户当前明确任务要求
2. `AGENTS.md`
3. `.ai_agents/00-core.md`
4. 其他 `.ai_agents/*.md` 模块规则
5. 项目内已有代码风格与相似实现

若规则之间出现冲突，应优先选择：

- 更保守的做法
- 更兼容现有项目的做法
- 更小改动的做法

---

## 按任务类型加载规则
执行任务时，至少按以下方式理解和应用规则：

### 所有任务都必须遵循
- `AGENTS.md`
- `.ai_agents/00-core.md`
- `.ai_agents/01-style.md`
- `.ai_agents/05-validation.md`
- `.ai_agents/06-output.md`
- `.ai_agents/07-forbidden.md`

### 涉及 Go 代码时，额外必须遵循
- `.ai_agents/02-go-rules.md`

### 涉及 HTTP API、Handler、Controller、Service 时，额外必须遵循
- `.ai_agents/03-api-rules.md`

### 涉及 Repository、DAO、数据库、SQL、ORM、数据映射时，额外必须遵循
- `.ai_agents/04-data-rules.md`

---

## 标准执行流程
在本项目中处理任何开发任务时，默认执行流程如下：

1. 阅读当前任务描述
2. 阅读 `AGENTS.md`
3. 根据任务类型匹配需要遵循的模块文件
4. 阅读相关代码上下文、调用链、相似实现
5. 按“最小必要改动”原则实施修改
6. 修改后执行格式化、自检、尽可能的编译/测试验证
7. 按 `.ai_agents/06-output.md` 输出真实结果说明

---

## 默认行为要求
除非任务明确要求，否则默认必须满足以下要求：

- 使用当前项目代码风格
- 方法、变量、API 命名延续系统现有风格
- 不过度优化设计架构
- 不做无关重构
- 不擅自修改公共接口、字段名、tag、错误语义
- 修改后必须仔细检查代码
- 修改后必须格式化代码
- 有条件时必须尽可能验证编译和测试
- 未执行的验证不得声称已通过

---

## 模块化执行要求
执行任务时，不得只参考单一规则文件而忽略相关模块。

具体要求如下：

- 代码修改类任务：必须同时遵循核心规则、风格规则、语言规则、验证规则、输出规则、禁止规则
- API 改动类任务：必须额外遵循 API 分层规则
- 数据访问类任务：必须额外遵循数据层规则
- 若一次任务同时涉及 API 与数据层，则相关模块规则同时生效
- 若无法确定适用模块，默认采用更严格、更保守的规则

---

## 风险控制要求
以下行为默认视为高风险，除非任务明确要求，否则不应执行：

- 大规模重构
- 修改目录结构
- 修改模块边界
- 修改公共接口
- 修改对外字段名或 tag
- 引入第三方依赖
- 改写并发模型
- 修改事务、幂等、重试、回滚逻辑
- 修改响应结构或错误语义

---

## 最终要求
完成任务后，必须：

- 按模块规则完成自检
- 进行格式化
- 尽可能执行编译/测试验证
- 真实说明改动内容和验证情况

不得把“未执行的检查”表述为“已通过”。
